import { EXPORT_TYPES_MODULE_ID, RESOLVED_EXPORT_TYPES_MODULE_ID, WORKER_EXPORT_TYPES_EVENT, exportTypesModuleSource } from "../export-types.js";
import { createPlugin } from "../factory.js";
import { resolvePluginApi } from "../utils.js";
//#region src/rolldown/plugins/virtual-modules.ts
const VIRTUAL_MODULE_REGEXP = /^\0?distilled:.*$/;
const WORKER_ENTRY_PREFIX = "\0distilled:worker-entry:";
const USER_ENTRY_PREFIX = "\0distilled:user-entry:";
const INJECT_PREFIX = "\0distilled:inject:";
/**
* The id of the generated Worker entry that wraps the user entry `id`.
*
* Entry ids are embedded into the specifier and round-tripped through
* Rolldown/Vite id handling, which expects POSIX-style separators. On Windows
* the input is an absolute path with `\` separators (e.g. `D:\app\entry.ts`),
* which would corrupt the virtual id and break resolution. Normalizing to
* forward slashes is a no-op on POSIX, where ids already use `/`.
*/
const workerEntryId = (id) => `${WORKER_ENTRY_PREFIX}${id.replace(/\\/g, "/")}`;
const virtualModulesPlugin = createPlugin("virtual-modules", (options) => {
	let unenvApi;
	const inject = () => {
		if (!unenvApi) return [];
		return [...unenvApi.polyfill.map((module) => `import "${module}";`), ...Object.keys(unenvApi.inject).map((injectedName) => `import "${INJECT_PREFIX}${injectedName}";`)];
	};
	return {
		vite: { enforce: "pre" },
		shared: {
			buildStart({ plugins }) {
				unenvApi = resolvePluginApi(plugins, "distilled-cloudflare:nodejs-unenv");
			},
			resolveId: {
				filter: { id: VIRTUAL_MODULE_REGEXP },
				handler(id) {
					if (id === "distilled:export-types" || id === RESOLVED_EXPORT_TYPES_MODULE_ID) return { id: RESOLVED_EXPORT_TYPES_MODULE_ID };
					if (id.startsWith("\0distilled:worker-entry:") || id.startsWith(INJECT_PREFIX)) return { id };
					if (id.startsWith(USER_ENTRY_PREFIX)) return this.resolve(id.slice(22), void 0, {
						isEntry: true,
						kind: "import-statement"
					});
				}
			},
			load: {
				filter: { id: VIRTUAL_MODULE_REGEXP },
				handler(id) {
					if (id.startsWith("\0distilled:worker-entry:")) {
						const userEntryId = id.replace(WORKER_ENTRY_PREFIX, USER_ENTRY_PREFIX);
						return [
							...inject(),
							...options.exports ? [`export { ${options.exports.join(", ")} } from "${userEntryId}";`] : [
								`import * as userEntry from "${userEntryId}";`,
								`export * from "${userEntryId}";`,
								`export default userEntry.default ?? {};`
							],
							"if (import.meta.hot) {",
							`  const { getExportTypes } = await import("${EXPORT_TYPES_MODULE_ID}");`,
							"  import.meta.hot.accept((module) => {",
							"    const exportTypes = getExportTypes(module);",
							`    import.meta.hot.send("${WORKER_EXPORT_TYPES_EVENT}", exportTypes);`,
							"  });",
							"}"
						].join("\n");
					}
					if (id === RESOLVED_EXPORT_TYPES_MODULE_ID) return exportTypesModuleSource;
					if (id.startsWith(INJECT_PREFIX)) {
						const injectedName = id.slice(18);
						const moduleSpecifier = unenvApi?.inject[injectedName];
						if (!moduleSpecifier) throw new Error(`Expected module specifier for "${injectedName}" to be defined`);
						return [`import ${injectedName} from "${moduleSpecifier}";`, `globalThis.${injectedName} = ${injectedName};`].join("\n");
					}
				}
			}
		}
	};
});
//#endregion
export { WORKER_ENTRY_PREFIX, virtualModulesPlugin, workerEntryId };

//# sourceMappingURL=virtual-modules.js.map