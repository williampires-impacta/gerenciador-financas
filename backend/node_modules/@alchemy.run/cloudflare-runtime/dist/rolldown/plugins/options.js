import { parseViteEnvironments } from "../options.js";
import { createPlugin } from "../factory.js";
import { hasNodejsCompat } from "../utils.js";
import { workerEntryId } from "./virtual-modules.js";
import path from "node:path";
import fs from "node:fs";
//#region src/rolldown/plugins/options.ts
const DEFAULT_RESOLVE_CONDITION_NAMES = [
	"workerd",
	"worker",
	"module",
	"browser"
];
const DEFAULT_RESOLVE_MAIN_FIELDS = [
	"browser",
	"module",
	"jsnext:main",
	"jsnext",
	"main"
];
const DEFAULT_RESOLVE_EXTENSIONS = [
	".mjs",
	".js",
	".mts",
	".ts",
	".jsx",
	".tsx",
	".json",
	".cjs",
	".cts",
	".ctx"
];
const TARGET = "es2024";
const optionsPlugin = createPlugin("options", (pluginOptions) => {
	let input = {};
	return {
		shared: { api: { input: () => input } },
		rolldown: { options(options) {
			input = normalizeInput(pluginOptions.main ?? options.input ?? {});
			options.input = wrapInput(input);
			options.preserveEntrySignatures ??= "strict";
			options.platform ??= "neutral";
			options.resolve ??= {};
			options.resolve.conditionNames ??= [...DEFAULT_RESOLVE_CONDITION_NAMES, "production"];
			options.resolve.mainFields ??= DEFAULT_RESOLVE_MAIN_FIELDS;
			options.resolve.extensions ??= DEFAULT_RESOLVE_EXTENSIONS;
			options.transform ??= {};
			options.transform.target ??= TARGET;
			options.transform.define ??= {};
			Object.assign(options.transform.define, getDefine(pluginOptions, "production"));
			return options;
		} },
		vite: { async config(userConfig) {
			const vite = await import("vite");
			const isRolldown = "rolldownVersion" in this.meta;
			const environmentNames = parseViteEnvironments(pluginOptions);
			const root = path.resolve(userConfig.root ?? ".");
			input = resolveInputPaths(normalizeInput(pluginOptions.main ?? defaultEnvironmentEntries(environmentNames[0], userConfig) ?? {}), root);
			const rollupOptions = {
				input: wrapInput(input),
				preserveEntrySignatures: "strict"
			};
			const define = getDefine(pluginOptions, process.env.NODE_ENV || userConfig.mode || "production");
			const makeEnvironment = ({ name, isEntry }) => {
				const entries = isEntry ? pluginOptions.main ?? defaultEnvironmentEntries(name, userConfig) : defaultEnvironmentEntries(name, userConfig) ?? pluginOptions.main;
				return {
					define,
					resolve: {
						noExternal: true,
						conditions: [...DEFAULT_RESOLVE_CONDITION_NAMES, "development|production"]
					},
					optimizeDeps: {
						noDiscovery: false,
						ignoreOutdatedRequests: true,
						entries: asArray(entries)?.map(vite.normalizePath),
						...isRolldown ? { rolldownOptions: {
							platform: "neutral",
							resolve: {
								conditionNames: [...DEFAULT_RESOLVE_CONDITION_NAMES, "development|production"],
								mainFields: DEFAULT_RESOLVE_MAIN_FIELDS,
								extensions: DEFAULT_RESOLVE_EXTENSIONS
							},
							transform: {
								target: TARGET,
								define
							}
						} } : { esbuildOptions: {
							platform: "neutral",
							conditions: [...DEFAULT_RESOLVE_CONDITION_NAMES, "development|production"],
							resolveExtensions: DEFAULT_RESOLVE_EXTENSIONS,
							mainFields: DEFAULT_RESOLVE_MAIN_FIELDS,
							target: TARGET,
							define
						} }
					},
					keepProcessEnv: true,
					...isEntry ? { build: {
						ssr: true,
						target: TARGET,
						emitAssets: true,
						copyPublicDir: false,
						outDir: getOutputDirectory(userConfig, name),
						...isRolldown ? { rolldownOptions: {
							...rollupOptions,
							platform: "neutral",
							resolve: {
								mainFields: DEFAULT_RESOLVE_MAIN_FIELDS,
								extensions: DEFAULT_RESOLVE_EXTENSIONS
							}
						} } : { rollupOptions }
					} } : {}
				};
			};
			const appType = userConfig.appType ?? (Object.keys(input).length === 0 ? "spa" : "custom");
			return {
				appType,
				builder: appType === "spa" || userConfig.builder?.buildApp !== void 0 ? void 0 : { buildApp: async (builder) => {
					for (const environment of Object.values(builder.environments)) {
						if (pluginOptions.skipEnvironments?.includes(environment.name)) continue;
						if (environment.isBuilt) continue;
						if (isEntrylessClientEnvironment(environment)) continue;
						await builder.build(environment);
					}
				} },
				ssr: {
					noExternal: true,
					resolve: { conditions: [...DEFAULT_RESOLVE_CONDITION_NAMES, "development|production"] }
				},
				environments: {
					client: { build: { outDir: getOutputDirectory(userConfig, "client") } },
					...Object.fromEntries(environmentNames.map((name, index) => [name, makeEnvironment({
						name,
						isEntry: index === 0
					})]))
				}
			};
		} }
	};
});
/**
* Vite always creates a default `client` environment, even for a pure Worker
* app that has no browser bundle. Building it makes Vite fall back to its
* default `index.html` entry, so the build fails on a file the app never
* referenced. Treat the environment as absent unless the app gave it an entry
* or serves an HTML shell.
*/
const isEntrylessClientEnvironment = (environment) => {
	if (environment.name !== "client") return false;
	const build = environment.config.build;
	if (build.rollupOptions?.input || build.rolldownOptions?.input) return false;
	return !fs.existsSync(path.join(environment.config.root, "index.html"));
};
const defaultEnvironmentEntries = (environmentName, userConfig) => {
	const environment = userConfig.environments?.[environmentName];
	return environment?.build?.rolldownOptions?.input ?? environment?.build?.rollupOptions?.input;
};
const asArray = (input) => {
	if (!input) return;
	if (typeof input === "string") return [input];
	if (Array.isArray(input) && input.length > 0) return input;
	return Object.values(input);
};
const normalizeInput = (input) => {
	if (typeof input === "string") return { [path.parse(input).name || "index"]: input };
	else if (Array.isArray(input)) return Object.fromEntries(input.map((p) => [path.parse(p).name, p]));
	else return input;
};
/**
* Resolve path-like input entries against the Vite root. Worker entries are
* round-tripped through a synthetic `\0distilled:user-entry:` id and resolved
* with no importer, so a relative input (`./workers/app.ts`) would otherwise
* resolve against `process.cwd()` — the wrong base whenever the build runs
* outside the project root (e.g. from a monorepo infra package). A bare
* specifier that names a real file under the root (`workers/app.ts`) is
* treated as a path input, matching Vite's own input semantics; anything else
* (virtual modules, package specifiers) passes through and resolves via
* plugins.
*/
const resolveInputPaths = (input, root) => Object.fromEntries(Object.entries(input).map(([key, id]) => [key, resolveInputPath(id, root)]));
const resolveInputPath = (id, root) => {
	if (path.isAbsolute(id)) return id;
	if (id.startsWith("./") || id.startsWith("../")) return path.resolve(root, id);
	const resolved = path.resolve(root, id);
	return fs.existsSync(resolved) ? resolved : id;
};
const wrapInput = (input) => Object.fromEntries(Object.entries(input).map(([key, id]) => [key, workerEntryId(id)]));
const getDefine = (options, nodeEnv) => {
	return {
		"process.env.NODE_ENV": JSON.stringify(nodeEnv),
		"global.process.env.NODE_ENV": JSON.stringify(nodeEnv),
		"globalThis.process.env.NODE_ENV": JSON.stringify(nodeEnv),
		...hasNodejsCompat(options.compatibilityFlags) ? {} : {
			"process.env": "{}",
			"global.process.env": "{}",
			"globalThis.process.env": "{}"
		},
		...options.compatibilityDate && options.compatibilityDate >= "2022-03-21" ? { "navigator.userAgent": "\"Cloudflare-Workers\"" } : {},
		...nodeEnv === "production" ? { "import.meta.hot": "false" } : {}
	};
};
const getOutputDirectory = (userConfig, environmentName) => {
	const rootOutputDirectory = userConfig.build?.outDir ?? "dist";
	return userConfig.environments?.[environmentName]?.build?.outDir ?? path.join(rootOutputDirectory, environmentName);
};
//#endregion
export { optionsPlugin };

//# sourceMappingURL=options.js.map