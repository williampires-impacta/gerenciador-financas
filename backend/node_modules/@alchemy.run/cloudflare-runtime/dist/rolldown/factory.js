import { isSkippedEnvironment } from "./options.js";
//#region src/rolldown/factory.ts
function createPlugin(pluginName, make) {
	const name = `distilled-cloudflare:${pluginName}`;
	return {
		rolldown: (options) => {
			const plugin = make(options);
			if (!plugin) return null;
			return {
				name,
				...plugin.shared,
				...plugin.rolldown
			};
		},
		vite: (options) => {
			const plugin = make(options);
			if (!plugin) return null;
			const vitePlugin = {
				name,
				sharedDuringBuild: true,
				...plugin.shared,
				...plugin.vite
			};
			const applyToEnvironment = vitePlugin.applyToEnvironment;
			vitePlugin.applyToEnvironment = function(environment) {
				if (isSkippedEnvironment(options, environment.name)) return false;
				return applyToEnvironment ? applyToEnvironment.call(this, environment) : true;
			};
			const configEnvironment = vitePlugin.configEnvironment;
			if (configEnvironment && options.skipEnvironments?.length) {
				const handler = typeof configEnvironment === "function" ? configEnvironment : configEnvironment.handler;
				const wrapped = function(environmentName, ...args) {
					if (options.skipEnvironments?.includes(environmentName)) return;
					return handler.call(this, environmentName, ...args);
				};
				vitePlugin.configEnvironment = typeof configEnvironment === "function" ? wrapped : {
					...configEnvironment,
					handler: wrapped
				};
			}
			return vitePlugin;
		}
	};
}
//#endregion
export { createPlugin };

//# sourceMappingURL=factory.js.map