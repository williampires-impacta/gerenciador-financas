//#region src/rolldown/utils.ts
const POSTFIX_REGEX = /[?#].*$/;
function sanitizePath(path) {
	return path.replace(POSTFIX_REGEX, "");
}
/**
* Normalizes a path to use forward slashes. This ensures paths are valid
* module specifiers and produce consistent output across platforms (e.g. Windows,
* where `path` APIs return backslash-separated paths).
*/
function toPosixPath(path) {
	return path.replace(/\\/g, "/");
}
function hasNodejsCompat(flags) {
	return flags?.some((flag) => flag === "nodejs_compat" || flag === "nodejs_compat_v2") ?? false;
}
function hasNodejsAls(flags) {
	return flags?.some((flag) => flag === "nodejs_als") ?? false;
}
function resolvePluginApi(plugins, name) {
	return plugins.find((plugin) => typeof plugin === "object" && plugin !== null && "name" in plugin && plugin.name === name)?.api;
}
//#endregion
export { hasNodejsAls, hasNodejsCompat, resolvePluginApi, sanitizePath, toPosixPath };

//# sourceMappingURL=utils.js.map