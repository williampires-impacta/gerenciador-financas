import { __exportAll } from "../_virtual/_rolldown/runtime.mjs";
import { Service } from "../Plugin.mjs";
import { HEADER_CF_BLOB } from "./CfOptions.shared.mjs";
import { LoopbackServer } from "./LoopbackServer.mjs";
import * as Effect from "effect/Effect";
import * as Layer from "effect/Layer";
//#region src/core/globals/Loopback.ts
var Loopback_exports = /* @__PURE__ */ __exportAll({
	Loopback: () => Loopback,
	LoopbackLive: () => LoopbackLive
});
var Loopback = class extends Service()("cloudflare-runtime/plugin/Loopback") {};
const LoopbackLive = Layer.effect(Loopback, Effect.gen(function* () {
	const loopbackServer = yield* LoopbackServer;
	return Loopback.of({
		services: [{
			name: "loopback:server",
			external: {
				address: loopbackServer.address,
				http: { cfBlobHeader: HEADER_CF_BLOB }
			}
		}, {
			name: "loopback:fetcher",
			worker: {
				compatibilityDate: "2025-01-01",
				modules: [{
					name: "loopback/fetcher.worker.js",
					esModule: `
              export default {
                async fetch(request, env, ctx) {
                  const headers = new Headers(request.headers);
                  headers.set("loopback-target", ctx.props.target);
                  headers.set("loopback-secret", env.SECRET);
                  return await env.LOOPBACK.fetch(request, { headers });
                }
              }
            `
				}],
				bindings: [{
					name: "LOOPBACK",
					service: { name: "loopback:server" }
				}, {
					name: "SECRET",
					text: loopbackServer.secret
				}]
			}
		}],
		api: { route: (target, handler) => loopbackServer.route(target, handler).pipe(Effect.map(() => ({
			name: "loopback:fetcher",
			props: { json: JSON.stringify({ target }) }
		}))) }
	});
}));
//#endregion
export { Loopback, LoopbackLive, Loopback_exports };

//# sourceMappingURL=Loopback.mjs.map