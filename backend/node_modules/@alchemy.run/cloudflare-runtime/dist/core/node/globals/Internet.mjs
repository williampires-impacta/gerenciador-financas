import { __exportAll } from "../_virtual/_rolldown/runtime.mjs";
import * as Context from "effect/Context";
import * as Effect from "effect/Effect";
import * as FileSystem from "effect/FileSystem";
import * as Layer from "effect/Layer";
import * as NodeTls from "node:tls";
//#region src/core/globals/Internet.ts
var Internet_exports = /* @__PURE__ */ __exportAll({
	Internet: () => Internet,
	InternetLive: () => InternetLive
});
var Internet = class extends Context.Service()("cloudflare-runtime/Internet") {};
const InternetLive = Layer.effect(Internet, Effect.gen(function* () {
	const fs = yield* FileSystem.FileSystem;
	const trustedCertificates = process.platform === "win32" ? Array.from(NodeTls.rootCertificates) : [];
	if (process.env.NODE_EXTRA_CA_CERTS !== void 0) {
		const certs = (yield* fs.readFileString(process.env.NODE_EXTRA_CA_CERTS).pipe(Effect.orElseSucceed(() => void 0)))?.match(/-----BEGIN CERTIFICATE-----[\s\S]+?-----END CERTIFICATE-----/g);
		if (certs) trustedCertificates.push(...certs);
	}
	return {
		name: "internet",
		network: {
			allow: [
				"public",
				"private",
				"240.0.0.0/4"
			],
			deny: [],
			tlsOptions: {
				trustBrowserCas: true,
				trustedCertificates
			}
		}
	};
}));
//#endregion
export { Internet, InternetLive, Internet_exports };

//# sourceMappingURL=Internet.mjs.map