//#region src/core/platform-proxy/PlatformProxyProtocol.shared.ts
/**
* Wire protocol shared between the Node-side platform proxy client
* ({@link ../platform-proxy/PlatformProxy.ts}) and the in-workerd proxy worker
* ({@link ./PlatformProxy.worker.ts}).
*
* The protocol is a deliberately small "magic proxy" (in the spirit of
* Miniflare's proxy client, reimplemented on our runtime):
*
* - `GET  /cdn-cgi/platform-proxy/env` — describe the worker's `env`:
*   plain values are serialised inline, everything else is a stub.
* - `POST /cdn-cgi/platform-proxy/call` — evaluate a method chain against a
*   binding (e.g. `KV.get("key")`, `DO.get(DO.idFromName("a")).increment()`)
*   and serialise the final result back.
* - `*    /cdn-cgi/platform-proxy/fetch` — raw HTTP passthrough to a
*   `fetch`-capable target (service bindings, Durable Object stubs) so
*   request/response bodies stream natively.
* - `POST /cdn-cgi/platform-proxy/cache/{match|put|delete}` — a small
*   in-memory Cache API emulation hosted by the proxy worker.
*
* Values crossing the boundary use the fully-tagged {@link EncodedValue}
* encoding: unambiguous, JSON-transportable, and easy to extend.
*/
const PLATFORM_PROXY_PREFIX = "/cdn-cgi/platform-proxy";
const PATH_ENV = `${PLATFORM_PROXY_PREFIX}/env`;
const PATH_CALL = `${PLATFORM_PROXY_PREFIX}/call`;
const PATH_FETCH = `${PLATFORM_PROXY_PREFIX}/fetch`;
const PATH_CACHE_MATCH = `${PLATFORM_PROXY_PREFIX}/cache/match`;
const PATH_CACHE_PUT = `${PLATFORM_PROXY_PREFIX}/cache/put`;
const PATH_CACHE_DELETE = `${PLATFORM_PROXY_PREFIX}/cache/delete`;
/** Text binding carrying the per-instance auth token into the proxy worker. */
const BINDING_PLATFORM_PROXY_TOKEN = "__PLATFORM_PROXY_TOKEN__";
const HEADER_TOKEN = "x-platform-proxy-token";
const HEADER_BINDING = "x-platform-proxy-binding";
const HEADER_CHAIN = "x-platform-proxy-chain";
const HEADER_URL = "x-platform-proxy-url";
const HEADER_RESULT = "x-platform-proxy-result";
const HEADER_BYTES_KIND = "x-platform-proxy-bytes-kind";
const HEADER_CACHE_NAME = "x-platform-proxy-cache-name";
const HEADER_CACHE_URL = "x-platform-proxy-cache-url";
const HEADER_CACHE_METHOD = "x-platform-proxy-cache-method";
const HEADER_CACHE_IGNORE_METHOD = "x-platform-proxy-cache-ignore-method";
const HEADER_CACHE_STATUS = "x-platform-proxy-cache-status";
const HEADER_CACHE_HEADERS = "x-platform-proxy-cache-headers";
var UnsupportedValueError = class extends Error {
	name = "UnsupportedValueError";
};
const bytesToBase64 = (bytes) => {
	let binary = "";
	const chunkSize = 8192;
	for (let index = 0; index < bytes.length; index += chunkSize) binary += String.fromCharCode(...bytes.subarray(index, index + chunkSize));
	return btoa(binary);
};
const base64ToBytes = (base64) => {
	const binary = atob(base64);
	const bytes = new Uint8Array(binary.length);
	for (let index = 0; index < binary.length; index++) bytes[index] = binary.charCodeAt(index);
	return bytes;
};
const describeValue = (value) => {
	if (value === null || typeof value !== "object") return typeof value;
	const name = value.constructor?.name;
	return name && name !== "Object" ? name : "object";
};
/**
* Encode a value into the tagged wire format. `encodeUnknown` lets each side
* contribute environment-specific encodings (native `DurableObjectId` in the
* worker, materialised ids and pending stub chains on the Node side) before
* the generic rules run.
*/
const encodeValue = (value, encodeUnknown) => {
	const custom = encodeUnknown?.(value);
	if (custom !== void 0) return custom;
	if (value === void 0) return { $: "undefined" };
	if (value === null) return { $: "null" };
	switch (typeof value) {
		case "boolean": return {
			$: "boolean",
			value
		};
		case "number":
			if (Number.isNaN(value)) return {
				$: "number",
				value: "NaN"
			};
			if (value === Infinity) return {
				$: "number",
				value: "Infinity"
			};
			if (value === -Infinity) return {
				$: "number",
				value: "-Infinity"
			};
			return {
				$: "number",
				value
			};
		case "bigint": return {
			$: "bigint",
			value: value.toString()
		};
		case "string": return {
			$: "string",
			value
		};
		default: break;
	}
	if (value instanceof Date) return {
		$: "date",
		value: value.getTime()
	};
	if (value instanceof ArrayBuffer) return {
		$: "bytes",
		kind: "arraybuffer",
		base64: bytesToBase64(new Uint8Array(value))
	};
	if (ArrayBuffer.isView(value)) {
		const bytes = new Uint8Array(value.buffer, value.byteOffset, value.byteLength);
		return {
			$: "bytes",
			kind: value instanceof Uint8Array ? "uint8array" : value.constructor.name,
			base64: bytesToBase64(bytes)
		};
	}
	if (value instanceof Error) return {
		$: "error",
		name: value.name,
		message: value.message,
		stack: value.stack
	};
	if (Array.isArray(value)) return {
		$: "array",
		value: value.map((entry) => encodeValue(entry, encodeUnknown))
	};
	if (typeof value === "object") {
		const prototype = Object.getPrototypeOf(value);
		if (prototype === Object.prototype || prototype === null) {
			const encoded = {};
			for (const [key, entry] of Object.entries(value)) encoded[key] = encodeValue(entry, encodeUnknown);
			return {
				$: "object",
				value: encoded
			};
		}
	}
	throw new UnsupportedValueError(`platform-proxy: cannot serialize a value of type "${describeValue(value)}" across the proxy boundary. Only JSON-compatible values, bytes, dates, errors, and DurableObjectIds are supported. If you awaited an intermediate stub (e.g. a Durable Object stub), call a method on it instead of awaiting it.`);
};
/**
* Decode a tagged wire value. `decodeUnknown` handles the environment-specific
* tags (`durable-object-id`, `chain`) and takes precedence over the generic
* rules; the generic decoder throws if one of those tags reaches it unhandled.
*/
const decodeValue = (encoded, decodeUnknown) => {
	const custom = decodeUnknown?.(encoded);
	if (custom !== void 0) return custom.value;
	switch (encoded.$) {
		case "undefined": return;
		case "null": return null;
		case "boolean":
		case "string": return encoded.value;
		case "number":
			if (encoded.value === "NaN") return NaN;
			if (encoded.value === "Infinity") return Infinity;
			if (encoded.value === "-Infinity") return -Infinity;
			return encoded.value;
		case "bigint": return BigInt(encoded.value);
		case "date": return new Date(encoded.value);
		case "bytes": {
			const bytes = base64ToBytes(encoded.base64);
			if (encoded.kind === "arraybuffer") return bytes.buffer.slice(bytes.byteOffset, bytes.byteOffset + bytes.byteLength);
			return bytes;
		}
		case "array": return encoded.value.map((entry) => decodeValue(entry, decodeUnknown));
		case "object": {
			const decoded = {};
			for (const [key, entry] of Object.entries(encoded.value)) decoded[key] = decodeValue(entry, decodeUnknown);
			return decoded;
		}
		case "error": {
			const error = new Error(encoded.message);
			error.name = encoded.name;
			if (encoded.stack !== void 0) error.stack = encoded.stack;
			return error;
		}
		case "durable-object-id":
		case "chain":
		case "r2-object": throw new UnsupportedValueError(`platform-proxy: unexpected "${encoded.$}" value in this context.`);
	}
};
//#endregion
export { BINDING_PLATFORM_PROXY_TOKEN, HEADER_BINDING, HEADER_BYTES_KIND, HEADER_CACHE_HEADERS, HEADER_CACHE_IGNORE_METHOD, HEADER_CACHE_METHOD, HEADER_CACHE_NAME, HEADER_CACHE_STATUS, HEADER_CACHE_URL, HEADER_CHAIN, HEADER_RESULT, HEADER_TOKEN, HEADER_URL, PATH_CACHE_DELETE, PATH_CACHE_MATCH, PATH_CACHE_PUT, PATH_CALL, PATH_ENV, PATH_FETCH, PLATFORM_PROXY_PREFIX, UnsupportedValueError, base64ToBytes, bytesToBase64, decodeValue, encodeValue };

//# sourceMappingURL=PlatformProxyProtocol.shared.mjs.map