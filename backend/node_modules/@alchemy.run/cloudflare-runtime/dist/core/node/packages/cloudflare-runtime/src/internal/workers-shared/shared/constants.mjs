//#region src/internal/workers-shared/shared/constants.ts
/**
* Maximum number of assets that can be deployed with a Worker; this is a global
* ceiling, and may vary by the user's subscription.
*/
const MAX_ASSET_COUNT = 1e5;
/** Maximum size per asset that can be deployed with a Worker */
const MAX_ASSET_SIZE = 25 * 1024 * 1024;
const CF_ASSETS_IGNORE_FILENAME = ".assetsignore";
const REDIRECTS_FILENAME = "_redirects";
const HEADERS_FILENAME = "_headers";
//#endregion
export { CF_ASSETS_IGNORE_FILENAME, HEADERS_FILENAME, MAX_ASSET_COUNT, MAX_ASSET_SIZE, REDIRECTS_FILENAME };

//# sourceMappingURL=constants.mjs.map