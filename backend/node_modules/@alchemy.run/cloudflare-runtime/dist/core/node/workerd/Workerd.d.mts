import { ConfigError, SystemError } from "../RuntimeError.shared.mjs";
import { Config } from "./Config.mjs";
import * as Context from "effect/Context";
import * as Effect from "effect/Effect";
import * as Layer from "effect/Layer";
import * as Scope from "effect/Scope";
declare namespace Workerd_d_exports {
  export { OutputSink, ServeOptions, Workerd, WorkerdLive, WorkerdPorts, isAddressInUseError };
}
interface WorkerdPorts {
  [socket: string]: number;
}
/**
 * Receives a decoded chunk of the workerd process's output along with the
 * stream it was written to.
 */
type OutputSink = (chunk: string, stream: "stdout" | "stderr") => void;
interface ServeOptions {
  /**
   * Capture the workerd process's output instead of piping it to the parent
   * process's stdout/stderr. Output produced before the process finishes
   * starting is not captured; startup failures surface as typed errors
   * instead.
   */
  readonly onOutput?: OutputSink;
}
declare const Workerd_base: Context.ServiceClass<Workerd, "cloudflare-runtime/workerd/Workerd", {
  readonly compatibilityDate: string;
  readonly serve: (config: Config, args?: Record<string, string | number | boolean>, options?: ServeOptions) => Effect.Effect<WorkerdPorts, ConfigError | SystemError, Scope.Scope>;
}>;
declare class Workerd extends Workerd_base {}
declare const WorkerdLive: Layer.Layer<Workerd, never, never>;
declare const isAddressInUseError: (error: ConfigError | SystemError) => boolean;
//#endregion
export { OutputSink, ServeOptions, Workerd, WorkerdLive, WorkerdPorts, Workerd_d_exports, isAddressInUseError };
//# sourceMappingURL=Workerd.d.mts.map