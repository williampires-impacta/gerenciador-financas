import { ConfigError } from "../../RuntimeError.shared.mjs";
import { use } from "../../PluginContext.mjs";
import { Service, use as use$1 } from "../../Plugin.mjs";
import { formatExtensionModule } from "../../internal/internal-modules.mjs";
import { loadInternalWorker } from "../../internal/internal-worker.mjs";
import * as Effect from "effect/Effect";
import * as Layer from "effect/Layer";
//#region src/core/bindings/hyperdrive/Hyperdrive.ts
const HyperdriveBindingWorker = { worker: () => loadInternalWorker("#cloudflare-runtime-core-worker/bindings/hyperdrive/hyperdrive-binding.worker") };
var Hyperdrive = class extends Service()("cloudflare-runtime/plugin/Hyperdrive") {};
const HyperdriveLive = Layer.succeed(Hyperdrive, Hyperdrive.of(use(({ worker: { hyperdrives } }) => {
	if (!hyperdrives || Object.keys(hyperdrives).length === 0) return Effect.succeed({ api: {} });
	return Effect.map(formatExtensionModule(HyperdriveBindingWorker), (esModule) => ({
		extensions: [{ modules: [{
			name: "cloudflare-runtime:hyperdrive",
			internal: true,
			esModule
		}] }],
		api: hyperdrives
	}));
})));
const local = (binding, hyperdriveId) => use$1(Hyperdrive, (hyperdrive) => hyperdrive.api[hyperdriveId] ? Effect.succeed({
	name: binding,
	wrapped: {
		moduleName: "cloudflare-runtime:hyperdrive",
		innerBindings: [{
			name: "ORIGIN",
			json: JSON.stringify(hyperdrive.api[hyperdriveId])
		}]
	}
}) : Effect.fail(new ConfigError({
	subtag: "HyperdriveOriginMissing",
	message: `No hyperdrive origin was provided for binding "${binding}" (id: ${hyperdriveId}).`,
	hint: `Add an entry for "${hyperdriveId}" to \`worker.hyperdrives\`.`,
	detail: {
		binding,
		hyperdriveId
	}
})));
//#endregion
export { Hyperdrive, HyperdriveLive, local };

//# sourceMappingURL=Hyperdrive.mjs.map