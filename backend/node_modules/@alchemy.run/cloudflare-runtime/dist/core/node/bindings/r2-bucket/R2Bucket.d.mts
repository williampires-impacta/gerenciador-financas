import { ServiceDesignator } from "../../workerd/Config.mjs";
import { BindingHook } from "../../PluginContext.mjs";
import { PluginBuilder } from "../../Plugin.mjs";
import { Storage } from "../../globals/Storage.mjs";
import { RemoteBindings } from "../../remote-bindings/RemoteBindings.mjs";
import { R2BucketProps, R2ServiceProps } from "./R2BucketOptions.shared.mjs";
import * as Effect from "effect/Effect";
import * as FileSystem from "effect/FileSystem";
import * as Layer from "effect/Layer";
import * as Path from "effect/Path";
//#region src/core/bindings/r2-bucket/R2Bucket.d.ts
declare const R2Bucket_base: import("effect/Context").ServiceClass<R2Bucket, "cloudflare-runtime/plugin/R2Bucket", PluginBuilder<{
  /**
   * Record that a bucket is in use (so the R2 services are only emitted
   * when at least one binding exists) and resolve the service designator
   * the binding should target: the shared `r2` service, with the bucket
   * name carried via designator props.
   */
  readonly register: (props: R2ServiceProps) => Effect.Effect<ServiceDesignator>;
}>>;
declare class R2Bucket extends R2Bucket_base {}
declare const R2BucketLive: Layer.Layer<R2Bucket, never, FileSystem.FileSystem | Path.Path | Storage>;
/**
 * Bind a local R2 bucket (`env.<binding>.get()` / `.put()` / `.list()` /
 * `.delete()` / multipart uploads).
 *
 * Data is persisted under `{storage}/r2`, keyed by the bucket id, so bindings
 * with the same `id` share data (including across workers and restarts when
 * disk-backed storage is configured).
 */
declare const local: (props: R2BucketProps) => BindingHook<R2Bucket>;
declare const remote: (binding: string, bucketName: string, jurisdiction?: string) => BindingHook<RemoteBindings>;
//#endregion
export { R2Bucket, R2BucketLive, local, remote };
//# sourceMappingURL=R2Bucket.d.mts.map