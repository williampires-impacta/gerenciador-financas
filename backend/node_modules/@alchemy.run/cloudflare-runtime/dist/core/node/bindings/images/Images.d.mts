import { ConfigError } from "../../RuntimeError.shared.mjs";
import { ServiceDesignator } from "../../workerd/Config.mjs";
import { BindingHook, PluginContext } from "../../PluginContext.mjs";
import { PluginBuilder } from "../../Plugin.mjs";
import { Storage } from "../../globals/Storage.mjs";
import { Loopback } from "../../globals/Loopback.mjs";
import { RemoteBindings } from "../../remote-bindings/RemoteBindings.mjs";
import { ImagesProps } from "./ImagesOptions.shared.mjs";
import * as Effect from "effect/Effect";
import * as FileSystem from "effect/FileSystem";
import * as Layer from "effect/Layer";
import * as Path from "effect/Path";
//#region src/core/bindings/images/Images.d.ts
declare const Images_base: import("effect/Context").ServiceClass<Images, "cloudflare-runtime/plugin/Images", PluginBuilder<{
  /**
   * Record that a local `images` binding is in use (so the images services
   * are only emitted when at least one binding exists), register the
   * node-side loopback route that runs Sharp transforms, and resolve the
   * service designator the wrapped `cloudflare-internal:images-api` binding
   * should target.
   */
  readonly register: () => Effect.Effect<ServiceDesignator, ConfigError, PluginContext | Loopback>;
}>>;
declare class Images extends Images_base {}
declare const ImagesLive: Layer.Layer<Images, never, FileSystem.FileSystem | Path.Path | Storage>;
/**
 * Bind a local Images simulator (`env.<binding>.input(...)/.info(...)` and the
 * hosted CRUD surface) for `alchemy dev`.
 *
 * The binding is workerd's real `cloudflare-internal:images-api` module
 * pointed at a local service: hosted images are stored in a KV-backed store
 * persisted under `{storage}/images` (variant URLs are served at
 * `/cdn-cgi/mf/imagedelivery/...` on the worker's own URL), and pixel work
 * (resize / rotate / transcode) runs in Node.js via Sharp over the loopback.
 *
 * Low fidelity, matching Miniflare's local mode: draws/overlays are ignored,
 * GIF and RGB/RGBA outputs fail with a 415 error (code 9520), and SVG inputs
 * are not transformed.
 */
declare const local: (props: ImagesProps) => BindingHook<Images | Loopback>;
/** Bind to the deployed Images service via the remote bindings proxy. */
declare const remote: (binding: string) => BindingHook<RemoteBindings>;
declare function imagesLocalFetcher(request: Request): Promise<Response>;
//#endregion
export { Images, ImagesLive, imagesLocalFetcher, local, remote };
//# sourceMappingURL=Images.d.mts.map