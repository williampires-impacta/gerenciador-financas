import { BindingHook } from "../../PluginContext.mjs";
import { PluginBuilder } from "../../Plugin.mjs";
import { RemoteBindings } from "../../remote-bindings/RemoteBindings.mjs";
import * as Layer from "effect/Layer";
//#region src/core/bindings/dispatch-namespace/DispatchNamespace.d.ts
declare const DispatchNamespace_base: import("effect/Context").ServiceClass<DispatchNamespace, "cloudflare-runtime/plugin/DispatchNamespace", PluginBuilder<never>>;
declare class DispatchNamespace extends DispatchNamespace_base {}
declare const DispatchNamespaceLive: Layer.Layer<DispatchNamespace, never, never>;
interface DispatchNamespaceProps {
  readonly binding: string;
  readonly namespace: string;
  readonly outbound?: {
    worker?: {
      service?: string;
      environment?: string;
    };
    params?: Array<{
      name: string;
    }>;
  };
}
/**
 * Bind to a deployed dispatch namespace via the remote bindings proxy.
 *
 * Wraps the remote-bindings client service so user code can call
 * `env.BINDING.get(name, args, options).fetch(...)` against a user worker
 * dispatched from the remote namespace.
 */
declare const remote: (props: DispatchNamespaceProps) => BindingHook<RemoteBindings | DispatchNamespace>;
//#endregion
export { DispatchNamespace, DispatchNamespaceLive, DispatchNamespaceProps, remote };
//# sourceMappingURL=DispatchNamespace.d.mts.map