//#region src/core/bindings/send-email/SendEmailOptions.shared.ts
const SERVICE_SEND_EMAIL = "send-email";
const SERVICE_SEND_EMAIL_STORAGE = "send-email:storage";
const SEND_EMAIL_ENTRYPOINT = "SendEmailBinding";
/** Disk service binding the send-email worker writes messages through. */
const BINDING_SEND_EMAIL_DISK = "DISK";
/**
* JSON binding carrying the node-side absolute path of the email persistence
* directory (`{storage}/email`), so logged file paths point at real files.
*/
const BINDING_SEND_EMAIL_DIRECTORY = "EMAIL_DIRECTORY";
/**
* Key under which the local `EmailMessage` shim stores the raw MIME message.
* Matches Miniflare's `RAW_EMAIL` constant (`workers/email/constants.ts`) —
* a plain string (not a symbol) so it survives JSRPC structured clone.
*/
const RAW_EMAIL = "EmailMessage::raw";
//#endregion
export { BINDING_SEND_EMAIL_DIRECTORY, BINDING_SEND_EMAIL_DISK, RAW_EMAIL, SEND_EMAIL_ENTRYPOINT, SERVICE_SEND_EMAIL, SERVICE_SEND_EMAIL_STORAGE };

//# sourceMappingURL=SendEmailOptions.shared.mjs.map