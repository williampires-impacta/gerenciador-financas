//#region src/core/bindings/d1/D1Options.shared.d.ts
/** Options for a local D1 database binding. */
interface D1Props {
  /** Binding name exposed on `env`. */
  readonly binding: string;
  /**
   * Database identifier, defaults to the binding name. Bindings with the same
   * identifier share data; the identifier also determines where data is
   * persisted on disk.
   */
  readonly id?: string;
}
/**
 * Service designator props passed to the D1 service entrypoint (`ctx.props`).
 * A single `d1` service hosts every database; each binding's designator
 * carries the database it should address.
 */
interface D1ServiceProps {
  readonly databaseId: string;
}
declare const SERVICE_D1 = "d1";
declare const SERVICE_D1_STORAGE = "d1:storage";
declare const D1_OBJECT_CLASS_NAME = "D1DatabaseObject";
declare const BINDING_D1_OBJECT = "OBJECT";
//#endregion
export { BINDING_D1_OBJECT, D1Props, D1ServiceProps, D1_OBJECT_CLASS_NAME, SERVICE_D1, SERVICE_D1_STORAGE };
//# sourceMappingURL=D1Options.shared.d.mts.map