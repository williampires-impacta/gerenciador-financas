import { ServiceDesignator } from "../../workerd/Config.mjs";
import { BindingHook } from "../../PluginContext.mjs";
import { PluginBuilder } from "../../Plugin.mjs";
import { Storage } from "../../globals/Storage.mjs";
import { RemoteBindings } from "../../remote-bindings/RemoteBindings.mjs";
import { KvNamespaceProps, KvServiceProps } from "./KvNamespaceOptions.shared.mjs";
import * as Effect from "effect/Effect";
import * as FileSystem from "effect/FileSystem";
import * as Layer from "effect/Layer";
import * as Path from "effect/Path";
//#region src/core/bindings/kv-namespace/KvNamespace.d.ts
declare const KvNamespace_base: import("effect/Context").ServiceClass<KvNamespace, "cloudflare-runtime/plugin/KvNamespace", PluginBuilder<{
  /**
   * Record that a namespace is in use (so the KV services are only emitted
   * when at least one binding exists) and resolve the service designator
   * the binding should target: the shared `kv` service, with the namespace
   * id carried via designator props.
   */
  readonly register: (props: KvServiceProps) => Effect.Effect<ServiceDesignator>;
}>>;
declare class KvNamespace extends KvNamespace_base {}
declare const KvNamespaceLive: Layer.Layer<KvNamespace, never, FileSystem.FileSystem | Path.Path | Storage>;
/**
 * Bind a local KV namespace (`env.<binding>.get()` / `.put()` / `.list()` /
 * `.delete()`).
 *
 * Data is persisted under `{storage}/kv`, keyed by the namespace id, so
 * bindings with the same `id` share data (including across workers and
 * restarts when disk-backed storage is configured).
 */
declare const local: (props: KvNamespaceProps) => BindingHook<KvNamespace>;
declare const remote: (binding: string, namespaceId: string) => BindingHook<RemoteBindings>;
//#endregion
export { KvNamespace, KvNamespaceLive, local, remote };
//# sourceMappingURL=KvNamespace.d.mts.map