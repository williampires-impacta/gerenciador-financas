//#region src/core/bindings/cache/CacheOptions.shared.d.ts
/**
 * Service designator props passed to the cache service entrypoint
 * (`ctx.props`) via the user worker's `cacheApiOutbound` designator. A single
 * `cache` service hosts every cache (default and named); when `enabled` is
 * false, the entry handler responds with no-ops instead of touching storage.
 */
interface CacheServiceProps {
  readonly enabled: boolean;
}
declare const SERVICE_CACHE = "cache";
declare const SERVICE_CACHE_STORAGE = "cache:storage";
declare const CACHE_OBJECT_CLASS_NAME = "CacheObject";
declare const BINDING_CACHE_OBJECT = "OBJECT";
declare const BINDING_CACHE_BLOBS = "BLOBS";
declare const BINDING_CACHE_ENABLE_CONTROL_ENDPOINTS = "ENABLE_CONTROL_ENDPOINTS";
/**
 * Header set by workerd on Cache API requests made through `caches.open(<n>)`
 * carrying the cache name; absent for `caches.default`.
 */
declare const HEADER_CACHE_NAMESPACE = "cf-cache-namespace";
/** Cache status (`HIT`/`MISS`) response header, observed by workerd. */
declare const HEADER_CACHE_STATUS = "cf-cache-status";
/**
 * Internal header carrying the cache object name (URI-encoded) from the entry
 * `fetch` handler to the Durable Object, which needs it to namespace blob
 * paths on disk.
 */
declare const HEADER_CACHE_OBJECT_NAME = "CF-Runtime-Cache-Name";
/**
 * Internal header marking a control operation (fake timers, storage
 * inspection). The request body is JSON `{ name, args }`. Only honoured when
 * control endpoints are enabled; used by tests.
 */
declare const HEADER_CACHE_CONTROL_OP = "CF-Runtime-Cache-Control-Op";
//#endregion
export { BINDING_CACHE_BLOBS, BINDING_CACHE_ENABLE_CONTROL_ENDPOINTS, BINDING_CACHE_OBJECT, CACHE_OBJECT_CLASS_NAME, CacheServiceProps, HEADER_CACHE_CONTROL_OP, HEADER_CACHE_NAMESPACE, HEADER_CACHE_OBJECT_NAME, HEADER_CACHE_STATUS, SERVICE_CACHE, SERVICE_CACHE_STORAGE };
//# sourceMappingURL=CacheOptions.shared.d.mts.map