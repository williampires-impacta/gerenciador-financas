import { ConfigError } from "../../RuntimeError.shared.mjs";
import { Service, use } from "../../Plugin.mjs";
import { Storage } from "../../globals/Storage.mjs";
import { formatInternalWorkerModules } from "../../internal/internal-modules.mjs";
import { loadInternalWorker } from "../../internal/internal-worker.mjs";
import { makeRemoteBinding } from "../../remote-bindings/RemoteBindings.mjs";
import { BINDING_D1_OBJECT, D1_OBJECT_CLASS_NAME, SERVICE_D1_STORAGE } from "./D1Options.shared.mjs";
import * as Effect from "effect/Effect";
import * as FileSystem from "effect/FileSystem";
import * as Layer from "effect/Layer";
import * as Path from "effect/Path";
//#region src/core/bindings/d1/D1.ts
const D1Worker = { worker: () => loadInternalWorker("#cloudflare-runtime-core-worker/bindings/d1/D1.worker") };
var D1 = class extends Service()("cloudflare-runtime/plugin/D1") {};
const D1Live = Layer.effect(D1, Effect.gen(function* () {
	const fs = yield* FileSystem.FileSystem;
	const path = yield* Path.Path;
	const storage = yield* Storage;
	const makeStorageService = Effect.gen(function* () {
		const storageDiskPath = "disk" in storage ? storage.disk?.path : void 0;
		if (!storageDiskPath) return yield* new ConfigError({
			subtag: "D1",
			message: "Cannot configure D1 persistence: the Storage service has no disk path.",
			hint: "Configure a disk-backed storage layer (`Storage.layerDisk` or `Storage.layerTemp`)."
		});
		const persistPath = path.join(storageDiskPath, "d1");
		yield* fs.makeDirectory(persistPath, { recursive: true }).pipe(Effect.mapError((cause) => new ConfigError({
			subtag: "D1",
			message: `Failed to create D1 persistence directory "${persistPath}": ${cause.message}`,
			hint: "Ensure the storage directory is writable.",
			detail: { persistPath },
			cause
		})));
		return {
			name: SERVICE_D1_STORAGE,
			disk: {
				path: persistPath,
				writable: true
			}
		};
	});
	return D1.of(Effect.sync(() => {
		let used = false;
		return {
			api: { register: (props) => Effect.sync(() => {
				used = true;
				return {
					name: "d1",
					props: { json: JSON.stringify(props) }
				};
			}) },
			defer: Effect.gen(function* () {
				if (!used) return {};
				return { services: [yield* makeStorageService, {
					name: "d1",
					worker: {
						compatibilityDate: "2025-01-01",
						modules: formatInternalWorkerModules(yield* Effect.promise(D1Worker.worker)),
						durableObjectNamespaces: [{
							className: D1_OBJECT_CLASS_NAME,
							enableSql: true,
							uniqueKey: `cloudflare-runtime-${D1_OBJECT_CLASS_NAME}`,
							preventEviction: true
						}],
						durableObjectStorage: { localDisk: SERVICE_D1_STORAGE },
						bindings: [{
							name: BINDING_D1_OBJECT,
							durableObjectNamespace: { className: D1_OBJECT_CLASS_NAME }
						}]
					}
				}] };
			})
		};
	}));
}));
/**
* Bind a local D1 database (`env.<binding>.prepare()` / `.batch()` /
* `.exec()`).
*
* The binding is the real `cloudflare-internal:d1-api` module wrapped around
* a fetcher to the local simulator. Data lives in Durable Object SQLite,
* persisted under `{storage}/d1` and keyed by the database id, so bindings
* with the same `id` share data (including across workers and restarts when
* disk-backed storage is configured).
*/
const local = (props) => use(D1, (d1) => Effect.map(d1.api.register({ databaseId: props.id ?? props.binding }), (service) => ({
	name: props.binding,
	wrapped: {
		moduleName: "cloudflare-internal:d1-api",
		innerBindings: [{
			name: "fetcher",
			service
		}]
	}
})));
const remote = (binding, id) => makeRemoteBinding({
	name: binding,
	type: "d1",
	id,
	raw: true
}, (service) => ({
	name: binding,
	wrapped: {
		moduleName: "cloudflare-internal:d1-api",
		innerBindings: [{
			name: "fetcher",
			service
		}]
	}
}));
//#endregion
export { D1, D1Live, local, remote };

//# sourceMappingURL=D1.mjs.map