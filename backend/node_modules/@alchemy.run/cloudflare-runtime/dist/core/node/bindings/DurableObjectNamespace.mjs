import { __exportAll } from "../_virtual/_rolldown/runtime.mjs";
import { ConfigError } from "../RuntimeError.shared.mjs";
import { defaultDurableObjectUniqueKey } from "../internal/constants.mjs";
import { PluginContext } from "../PluginContext.mjs";
import { RegistryProxy } from "../registry/RegistryProxy.mjs";
import * as Effect from "effect/Effect";
//#region src/core/bindings/DurableObjectNamespace.ts
var DurableObjectNamespace_exports = /* @__PURE__ */ __exportAll({ local: () => local });
/**
* Bind a Durable Object namespace.
*
* - Without `scriptName`: bind a DO defined on the current worker.
* - With `scriptName === <current worker name>`: same as above.
* - With `scriptName !== <current worker name>`: route through the dev
*   registry proxy so the DO can live in a different `cloudflare-runtime`
*   or `wrangler dev` process.
*/
const local = ({ binding, className, scriptName, uniqueKey }) => PluginContext.use((context) => {
	if (scriptName === void 0 || scriptName === context.worker.name) {
		const namespace = context.worker.durableObjectNamespaces?.find((namespace) => namespace.className === className);
		if (!namespace) return Effect.fail(new ConfigError({
			subtag: "DurableObjectNamespaceNotFound",
			message: `Durable object namespace ${className} not found`,
			hint: `Make sure the durable object namespace ${className} is defined in the worker config`,
			detail: {
				className,
				registeredClasses: context.worker.durableObjectNamespaces?.map((namespace) => namespace.className)
			}
		}));
		if (uniqueKey && namespace.uniqueKey !== uniqueKey) return Effect.fail(new ConfigError({
			subtag: "DurableObjectNamespaceUniqueKeyMismatch",
			message: `Durable object namespace ${className} has unique key "${namespace.uniqueKey}" but "${uniqueKey}" was provided`
		}));
		return Effect.succeed({
			name: binding,
			durableObjectNamespace: { className }
		});
	}
	return context.get(RegistryProxy).pipe(Effect.flatMap((proxy) => proxy.api.subscribe({
		kind: "durable-object",
		scriptName,
		className,
		uniqueKey: uniqueKey ?? defaultDurableObjectUniqueKey(scriptName, className)
	})), Effect.map((durableObjectNamespace) => ({
		name: binding,
		durableObjectNamespace
	})));
});
//#endregion
export { DurableObjectNamespace_exports, local };

//# sourceMappingURL=DurableObjectNamespace.mjs.map