import { SystemError } from "../RuntimeError.shared.mjs";
import { Paths } from "../internal/Paths.mjs";
import { RegistryEntry, ResolvedTargetMap, Subscriber } from "./RegistryTypes.shared.mjs";
import * as Context from "effect/Context";
import * as Effect from "effect/Effect";
import * as FileSystem from "effect/FileSystem";
import * as Layer from "effect/Layer";
import * as Path from "effect/Path";
import * as Stream from "effect/Stream";
import * as Scope from "effect/Scope";
declare namespace Registry_d_exports {
  export { Registry, RegistryLive };
}
declare const Registry_base: Context.ServiceClass<Registry, "cloudflare-runtime/registry/Registry", {
  /**
   * Reads the registry and returns the resolved targets for the given subscribers.
   */
  readonly read: (subscribers: ReadonlyArray<Subscriber>) => Effect.Effect<ResolvedTargetMap>;
  /**
   * Subscribes to changes in the registry for the given subscribers.
   * Returns a stream containing an updated `ResolvedTargetMap` whenever the registry changes.
   */
  readonly subscribe: (subscribers: ReadonlyArray<Subscriber>) => Stream.Stream<ResolvedTargetMap>;
  /**
   * Writes an entry to the registry.
   * The entry is removed when the scope closes.
   */
  readonly write: (entry: RegistryEntry) => Effect.Effect<void, SystemError, Scope.Scope>;
}>;
declare class Registry extends Registry_base {}
declare const RegistryLive: Layer.Layer<Registry, import("effect/PlatformError").PlatformError | SystemError, FileSystem.FileSystem | Path.Path | Paths>;
//#endregion
export { Registry, RegistryLive, Registry_d_exports };
//# sourceMappingURL=Registry.d.mts.map