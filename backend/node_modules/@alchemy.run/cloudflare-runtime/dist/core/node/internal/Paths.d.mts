import { SystemError } from "../RuntimeError.shared.mjs";
import * as Context from "effect/Context";
import * as Effect from "effect/Effect";
import * as FileSystem from "effect/FileSystem";
import * as Layer from "effect/Layer";
import * as Path from "effect/Path";
//#region src/core/internal/Paths.d.ts
declare const Paths_base: Context.ServiceClass<Paths, "cloudflare-runtime/Paths", {
  readonly cache: (...prefix: Array<string>) => Effect.Effect<string, SystemError>;
  readonly config: (...prefix: Array<string>) => Effect.Effect<string, SystemError>;
  readonly data: (...prefix: Array<string>) => Effect.Effect<string, SystemError>;
  readonly state: (...prefix: Array<string>) => Effect.Effect<string, SystemError>;
}>;
declare class Paths extends Paths_base {}
declare const PathsLive: Layer.Layer<Paths, never, FileSystem.FileSystem | Path.Path>;
declare const data: (...prefix: Array<string>) => Effect.Effect<string, SystemError, Paths>;
declare const config: (...prefix: Array<string>) => Effect.Effect<string, SystemError, Paths>;
declare const cache: (...prefix: Array<string>) => Effect.Effect<string, SystemError, Paths>;
declare const state: (...prefix: Array<string>) => Effect.Effect<string, SystemError, Paths>;
//#endregion
export { Paths, PathsLive, cache, config, data, state };
//# sourceMappingURL=Paths.d.mts.map