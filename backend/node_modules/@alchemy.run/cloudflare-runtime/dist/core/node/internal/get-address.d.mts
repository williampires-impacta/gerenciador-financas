import { SystemError } from "../RuntimeError.shared.mjs";
import * as Effect from "effect/Effect";
import * as NodeHttp from "node:http";
//#region src/core/internal/get-address.d.ts
/** Maps listen-all/unspecified hosts to a loopback host that is safe to connect to on every platform. */
declare const toConnectableHost: (host: string) => string;
declare const getAddress: (server: NodeHttp.Server) => Effect.Effect<string, SystemError>;
//#endregion
export { getAddress, toConnectableHost };
//# sourceMappingURL=get-address.d.mts.map