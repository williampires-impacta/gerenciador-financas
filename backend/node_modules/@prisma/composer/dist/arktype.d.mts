import { n as SecretString } from "./secret-CHOw4LBU-C8yXoC9N.mjs";
//#region ../../0-framework/0-foundation/foundation/dist/arktype.d.mts
//#region src/arktype.d.ts
/**
 * A schema leaf that accepts only a redacting `SecretString` box, so
 * `input().<field>.expose()` type-checks and binding a plain literal there
 * fails the deploy (ADR-0042).
 *
 * ```ts
 * input: type({ signingKey: secretString() })
 * ```
 */
declare const secretString: () => import("arktype/internal/variants/object.ts").ObjectType<SecretString, {}>;
//#endregion
export { secretString };
//# sourceMappingURL=arktype.d.mts.map