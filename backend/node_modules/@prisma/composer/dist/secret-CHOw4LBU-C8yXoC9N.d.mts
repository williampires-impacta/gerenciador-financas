//#region ../../0-framework/0-foundation/foundation/dist/secret-CHOw4LBU.d.mts
//#region src/secret.d.ts
declare const SECRET_BOX: unique symbol;
declare class SecretBox<T> {
  #private;
  readonly [SECRET_BOX] = true;
  constructor(value: T);
  /** The sole explicit door to the wrapped value. */
  expose(): T;
  toString(): string;
  toJSON(): string;
  valueOf(): string;
  [Symbol.toPrimitive](): string;
}
/** The common case: a secret string. */
type SecretString = SecretBox<string>;
/**
 * True for a redacting secret box — what a schema's secret leaf checks
 * (ADR-0042). Reads the brand rather than `instanceof`, so a box from a
 * duplicated module copy in a bundle counts and a look-alike does not.
 */
declare function isSecretString(value: unknown): value is SecretString;
//#endregion
export { SecretString as n, isSecretString as r, SecretBox as t };
//# sourceMappingURL=secret-CHOw4LBU-C8yXoC9N.d.mts.map