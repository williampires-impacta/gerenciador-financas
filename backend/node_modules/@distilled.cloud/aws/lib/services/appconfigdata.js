import * as HttpClient from "effect/unstable/http/HttpClient";
import * as S from "@distilled.cloud/core/schema";
import * as API from "@distilled.cloud/core/api";
import { AwsProtocol } from "../protocol.js";
import { Retry } from "../retry.js";
import * as T from "../traits.js";
import * as C from "../category.js";
const svc = T.AwsApiService({
    sdkId: "AppConfigData",
    serviceShapeName: "AppConfigData",
});
const auth = T.AwsAuthSigv4({ name: "appconfig" });
const ver = T.ServiceVersion("2021-11-11");
const proto = T.AwsProtocolsRestJson1();
const rules = T.EndpointResolver((p, _) => {
    const { Region, UseDualStack = false, UseFIPS = false, Endpoint } = p;
    const e = (u, p = {}, h = {}) => ({
        type: "endpoint",
        endpoint: { url: u, properties: p, headers: h },
    });
    const err = (m) => ({
        type: "error",
        message: m,
    });
    if (Endpoint != null) {
        if (UseFIPS === true) {
            return err("Invalid Configuration: FIPS and custom endpoint are not supported");
        }
        if (UseDualStack === true) {
            return err("Invalid Configuration: Dualstack and custom endpoint are not supported");
        }
        return e(Endpoint);
    }
    if (Region != null) {
        {
            const PartitionResult = _.partition(Region);
            if (PartitionResult != null && PartitionResult !== false) {
                if (UseFIPS === true && UseDualStack === true) {
                    if (true === _.getAttr(PartitionResult, "supportsFIPS") &&
                        true === _.getAttr(PartitionResult, "supportsDualStack")) {
                        return e(`https://appconfigdata-fips.${Region}.${_.getAttr(PartitionResult, "dualStackDnsSuffix")}`);
                    }
                    return err("FIPS and DualStack are enabled, but this partition does not support one or both");
                }
                if (UseFIPS === true) {
                    if (_.getAttr(PartitionResult, "supportsFIPS") === true) {
                        if (_.getAttr(PartitionResult, "name") === "aws-us-gov") {
                            return e(`https://appconfigdata.${Region}.amazonaws.com`);
                        }
                        return e(`https://appconfigdata-fips.${Region}.${_.getAttr(PartitionResult, "dnsSuffix")}`);
                    }
                    return err("FIPS is enabled but this partition does not support FIPS");
                }
                if (UseDualStack === true) {
                    if (true === _.getAttr(PartitionResult, "supportsDualStack")) {
                        return e(`https://appconfigdata.${Region}.${_.getAttr(PartitionResult, "dualStackDnsSuffix")}`);
                    }
                    return err("DualStack is enabled but this partition does not support DualStack");
                }
                return e(`https://appconfigdata.${Region}.${_.getAttr(PartitionResult, "dnsSuffix")}`);
            }
        }
    }
    return err("Invalid Configuration: Missing Region");
});
export class BadRequestException extends /*@__PURE__*/ /*@__PURE__*/ S.TaggedError()("BadRequestException", {
    message: S.optional(S.String).pipe(T.ErrorMessage()),
    Reason: S.optional(S.String),
    Details: S.optional(S.suspend(() => BadRequestDetails).annotate({
        identifier: "BadRequestDetails",
    })),
}, T.HttpError(400)).pipe(C.withBadRequestError) {
}
export class InternalServerException extends /*@__PURE__*/ /*@__PURE__*/ S.TaggedError()("InternalServerException", { message: S.optional(S.String).pipe(T.ErrorMessage()) }, T.HttpError(500)).pipe(C.withServerError) {
}
export class ResourceNotFoundException extends /*@__PURE__*/ /*@__PURE__*/ S.TaggedError()("ResourceNotFoundException", {
    message: S.optional(S.String).pipe(T.ErrorMessage()),
    ResourceType: S.optional(S.String),
    ReferencedBy: S.optional(S.suspend(() => StringMap).annotate({ identifier: "StringMap" })),
}, T.HttpError(404)).pipe(C.withBadRequestError) {
}
export class ThrottlingException extends /*@__PURE__*/ /*@__PURE__*/ S.TaggedError()("ThrottlingException", { message: S.optional(S.String).pipe(T.ErrorMessage()) }, T.HttpError(429)).pipe(C.withThrottlingError) {
}
export const GetLatestConfigurationRequest = /*@__PURE__*/ S.suspend(() => S.Struct({
    ConfigurationToken: S.String.pipe(T.HttpQuery("configuration_token")),
}).pipe(T.all(T.Http({ method: "GET", uri: "/configuration" }), svc, auth, proto, ver, rules))).annotate({
    identifier: "GetLatestConfigurationRequest",
});
export const GetLatestConfigurationResponse = /*@__PURE__*/ S.suspend(() => S.Struct({
    NextPollConfigurationToken: S.optional(S.String).pipe(T.HttpHeader("Next-Poll-Configuration-Token")),
    NextPollIntervalInSeconds: S.optional(S.Number).pipe(T.HttpHeader("Next-Poll-Interval-In-Seconds")),
    ContentType: S.optional(S.String).pipe(T.HttpHeader("Content-Type")),
    Configuration: S.optional(T.StreamingOutput).pipe(T.HttpPayload()),
    VersionLabel: S.optional(S.String).pipe(T.HttpHeader("Version-Label")),
})).annotate({
    identifier: "GetLatestConfigurationResponse",
});
export const StartConfigurationSessionRequest = /*@__PURE__*/ S.suspend(() => S.Struct({
    ApplicationIdentifier: S.String,
    EnvironmentIdentifier: S.String,
    ConfigurationProfileIdentifier: S.String,
    RequiredMinimumPollIntervalInSeconds: S.optional(S.Number),
}).pipe(T.all(T.Http({ method: "POST", uri: "/configurationsessions" }), svc, auth, proto, ver, rules))).annotate({
    identifier: "StartConfigurationSessionRequest",
});
export const StartConfigurationSessionResponse = /*@__PURE__*/ S.suspend(() => S.Struct({ InitialConfigurationToken: S.optional(S.String) })).annotate({
    identifier: "StartConfigurationSessionResponse",
});
export const InvalidParameterDetail = /*@__PURE__*/ S.suspend(() => S.Struct({ Problem: S.optional(S.String) })).annotate({
    identifier: "InvalidParameterDetail",
});
export const InvalidParameterMap = /*@__PURE__*/ S.Record(S.String, InvalidParameterDetail.pipe(S.optional));
export const BadRequestDetails = /*@__PURE__*/ S.Union([
    S.Struct({ InvalidParameters: InvalidParameterMap }),
]);
export const StringMap = /*@__PURE__*/ S.Record(S.String, S.String.pipe(S.optional));
/**
 * Retrieves the latest deployed configuration. This API may return empty configuration
 * data if the client already has the latest version. For more information about this API
 * action and to view example CLI commands that show how to use it with the StartConfigurationSession API action, see Retrieving the
 * configuration in the *AppConfig User Guide*.
 *
 * Note the following important information.
 *
 * - Each configuration token is only valid for one call to
 * `GetLatestConfiguration`. The `GetLatestConfiguration`
 * response includes a `NextPollConfigurationToken` that should always
 * replace the token used for the just-completed call in preparation for the next
 * one.
 *
 * - `GetLatestConfiguration` is a priced call. For more information, see
 * Pricing.
 */
export const getLatestConfiguration = /*@__PURE__*/ API.make(() => ({
    input: GetLatestConfigurationRequest,
    output: GetLatestConfigurationResponse,
    errors: [
        BadRequestException,
        InternalServerException,
        ResourceNotFoundException,
        ThrottlingException,
    ],
    protocol: AwsProtocol,
    retry: Retry,
    operationName: "GetLatestConfiguration",
}));
/**
 * Starts a configuration session used to retrieve a deployed configuration. For more
 * information about this API action and to view example CLI commands that show how to use
 * it with the GetLatestConfiguration API action, see Retrieving the
 * configuration in the *AppConfig User Guide*.
 */
export const startConfigurationSession = /*@__PURE__*/ API.make(() => ({
    input: StartConfigurationSessionRequest,
    output: StartConfigurationSessionResponse,
    errors: [
        BadRequestException,
        InternalServerException,
        ResourceNotFoundException,
        ThrottlingException,
    ],
    protocol: AwsProtocol,
    retry: Retry,
    operationName: "StartConfigurationSession",
}));
//# sourceMappingURL=appconfigdata.js.map