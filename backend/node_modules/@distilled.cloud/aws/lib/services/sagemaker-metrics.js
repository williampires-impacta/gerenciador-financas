import * as HttpClient from "effect/unstable/http/HttpClient";
import * as S from "@distilled.cloud/core/schema";
import * as API from "@distilled.cloud/core/api";
import { AwsProtocol } from "../protocol.js";
import { Retry } from "../retry.js";
import * as T from "../traits.js";
const svc = T.AwsApiService({
    sdkId: "SageMaker Metrics",
    serviceShapeName: "SageMakerMetricsService",
});
const auth = T.AwsAuthSigv4({ name: "sagemaker" });
const ver = T.ServiceVersion("2022-09-30");
const proto = T.AwsProtocolsRestJson1();
const rules = T.EndpointResolver((p, _) => {
    const { Region, UseDualStack = false, UseFIPS = false, Endpoint } = p;
    const e = (u, p = {}, h = {}) => ({
        type: "endpoint",
        endpoint: { url: u, properties: p, headers: h },
    });
    const err = (m) => ({
        type: "error",
        message: m,
    });
    if (Endpoint != null) {
        if (UseFIPS === true) {
            return err("Invalid Configuration: FIPS and custom endpoint are not supported");
        }
        if (UseDualStack === true) {
            return err("Invalid Configuration: Dualstack and custom endpoint are not supported");
        }
        return e(Endpoint);
    }
    if (Region != null) {
        {
            const PartitionResult = _.partition(Region);
            if (PartitionResult != null && PartitionResult !== false) {
                if (UseFIPS === true && UseDualStack === true) {
                    if (true === _.getAttr(PartitionResult, "supportsFIPS") &&
                        true === _.getAttr(PartitionResult, "supportsDualStack")) {
                        return e(`https://metrics.sagemaker-fips.${Region}.${_.getAttr(PartitionResult, "dualStackDnsSuffix")}`);
                    }
                    return err("FIPS and DualStack are enabled, but this partition does not support one or both");
                }
                if (UseFIPS === true) {
                    if (_.getAttr(PartitionResult, "supportsFIPS") === true) {
                        if (_.getAttr(PartitionResult, "name") === "aws") {
                            return e(`https://metrics-fips.sagemaker.${Region}.amazonaws.com`);
                        }
                        return e(`https://metrics.sagemaker-fips.${Region}.${_.getAttr(PartitionResult, "dnsSuffix")}`);
                    }
                    return err("FIPS is enabled but this partition does not support FIPS");
                }
                if (UseDualStack === true) {
                    if (true === _.getAttr(PartitionResult, "supportsDualStack")) {
                        return e(`https://metrics.sagemaker.${Region}.${_.getAttr(PartitionResult, "dualStackDnsSuffix")}`);
                    }
                    return err("DualStack is enabled but this partition does not support DualStack");
                }
                return e(`https://metrics.sagemaker.${Region}.${_.getAttr(PartitionResult, "dnsSuffix")}`);
            }
        }
    }
    return err("Invalid Configuration: Missing Region");
});
export const MetricStatistic = /*@__PURE__*/ S.String;
export const Period = /*@__PURE__*/ S.String;
export const XAxisType = /*@__PURE__*/ S.String;
export const MetricQuery = /*@__PURE__*/ S.suspend(() => S.Struct({
    MetricName: S.optional(S.String),
    ResourceArn: S.optional(S.String),
    MetricStat: S.optional(MetricStatistic),
    Period: S.optional(Period),
    XAxisType: S.optional(XAxisType),
    Start: S.optional(S.Number),
    End: S.optional(S.Number),
})).annotate({ identifier: "MetricQuery" });
export const MetricQueryList = /*@__PURE__*/ S.Array(MetricQuery);
export const BatchGetMetricsRequest = /*@__PURE__*/ S.suspend(() => S.Struct({ MetricQueries: S.optional(MetricQueryList) }).pipe(T.all(T.Http({ method: "POST", uri: "/BatchGetMetrics" }), svc, auth, proto, ver, rules))).annotate({
    identifier: "BatchGetMetricsRequest",
});
export const MetricQueryResultStatus = /*@__PURE__*/ S.String;
export const XAxisValues = /*@__PURE__*/ S.Array(S.Number);
export const MetricValues = /*@__PURE__*/ S.Array(S.Number);
export const MetricQueryResult = /*@__PURE__*/ S.suspend(() => S.Struct({
    Status: S.optional(MetricQueryResultStatus),
    Message: S.optional(S.String),
    XAxisValues: S.optional(XAxisValues),
    MetricValues: S.optional(MetricValues),
})).annotate({
    identifier: "MetricQueryResult",
});
export const MetricQueryResultList = /*@__PURE__*/ S.Array(MetricQueryResult);
export const BatchGetMetricsResponse = /*@__PURE__*/ S.suspend(() => S.Struct({ MetricQueryResults: S.optional(MetricQueryResultList) })).annotate({
    identifier: "BatchGetMetricsResponse",
});
export const RawMetricData = /*@__PURE__*/ S.suspend(() => S.Struct({
    MetricName: S.optional(S.String),
    Timestamp: S.optional(S.Date.pipe(T.TimestampFormat("epoch-seconds"))),
    Step: S.optional(S.Number),
    Value: S.optional(S.Number),
})).annotate({ identifier: "RawMetricData" });
export const RawMetricDataList = /*@__PURE__*/ S.Array(RawMetricData);
export const BatchPutMetricsRequest = /*@__PURE__*/ S.suspend(() => S.Struct({
    TrialComponentName: S.optional(S.String),
    MetricData: S.optional(RawMetricDataList),
}).pipe(T.all(T.Http({ method: "PUT", uri: "/BatchPutMetrics" }), svc, auth, proto, ver, rules))).annotate({
    identifier: "BatchPutMetricsRequest",
});
export const PutMetricsErrorCode = /*@__PURE__*/ S.String;
export const BatchPutMetricsError_ = /*@__PURE__*/ S.suspend(() => S.Struct({
    Code: S.optional(PutMetricsErrorCode),
    MetricIndex: S.optional(S.Number),
})).annotate({
    identifier: "BatchPutMetricsError",
});
export const BatchPutMetricsErrorList = /*@__PURE__*/ S.Array(BatchPutMetricsError_);
export const BatchPutMetricsResponse = /*@__PURE__*/ S.suspend(() => S.Struct({ Errors: S.optional(BatchPutMetricsErrorList) })).annotate({
    identifier: "BatchPutMetricsResponse",
});
/**
 * Used to retrieve training metrics from SageMaker.
 */
export const batchGetMetrics = /*@__PURE__*/ API.make(() => ({
    input: BatchGetMetricsRequest,
    output: BatchGetMetricsResponse,
    errors: [],
    protocol: AwsProtocol,
    retry: Retry,
    operationName: "BatchGetMetrics",
}));
/**
 * Used to ingest training metrics into SageMaker. These metrics can be visualized in SageMaker Studio.
 */
export const batchPutMetrics = /*@__PURE__*/ API.make(() => ({
    input: BatchPutMetricsRequest,
    output: BatchPutMetricsResponse,
    errors: [],
    protocol: AwsProtocol,
    retry: Retry,
    operationName: "BatchPutMetrics",
}));
//# sourceMappingURL=sagemaker-metrics.js.map