import * as HttpClient from "effect/unstable/http/HttpClient";
import * as S from "@distilled.cloud/core/schema";
import * as API from "@distilled.cloud/core/api";
import { AwsProtocol } from "../protocol.js";
import { Retry } from "../retry.js";
import * as T from "../traits.js";
const ns = T.XmlNamespace("http://dynamodb.amazonaws.com/doc/2012-08-10/");
const svc = T.AwsApiService({
    sdkId: "DynamoDB Streams",
    serviceShapeName: "DynamoDBStreams_20120810",
});
const auth = T.AwsAuthSigv4({ name: "dynamodb" });
const ver = T.ServiceVersion("2012-08-10");
const proto = T.AwsProtocolsAwsJson1_0();
const rules = T.EndpointResolver((p, _) => {
    const { UseDualStack = false, UseFIPS = false, Endpoint, Region } = p;
    const e = (u, p = {}, h = {}) => ({
        type: "endpoint",
        endpoint: { url: u, properties: p, headers: h },
    });
    const err = (m) => ({
        type: "error",
        message: m,
    });
    if (Endpoint != null) {
        if (UseFIPS === true) {
            return err("Invalid Configuration: FIPS and custom endpoint are not supported");
        }
        if (UseDualStack === true) {
            return err("Invalid Configuration: Dualstack and custom endpoint are not supported");
        }
        return e(Endpoint);
    }
    if (Region != null) {
        {
            const PartitionResult = _.partition(Region);
            if (PartitionResult != null && PartitionResult !== false) {
                if (_.getAttr(PartitionResult, "name") === "aws" &&
                    UseFIPS === false &&
                    UseDualStack === true) {
                    return e(`https://streams-dynamodb.${Region}.${_.getAttr(PartitionResult, "dualStackDnsSuffix")}`);
                }
                if (_.getAttr(PartitionResult, "name") === "aws" &&
                    UseFIPS === true &&
                    UseDualStack === true) {
                    return e(`https://streams-dynamodb-fips.${Region}.${_.getAttr(PartitionResult, "dualStackDnsSuffix")}`);
                }
                if (_.getAttr(PartitionResult, "name") === "aws-cn" &&
                    UseFIPS === false &&
                    UseDualStack === true) {
                    return e(`https://streams-dynamodb.${Region}.${_.getAttr(PartitionResult, "dualStackDnsSuffix")}`);
                }
                if (_.getAttr(PartitionResult, "name") === "aws-cn" &&
                    UseFIPS === true &&
                    UseDualStack === true) {
                    return e(`https://streams-dynamodb-fips.${Region}.${_.getAttr(PartitionResult, "dualStackDnsSuffix")}`);
                }
                if (_.getAttr(PartitionResult, "name") === "aws-us-gov" &&
                    UseFIPS === false &&
                    UseDualStack === true) {
                    return e(`https://streams-dynamodb.${Region}.${_.getAttr(PartitionResult, "dualStackDnsSuffix")}`);
                }
                if (_.getAttr(PartitionResult, "name") === "aws-us-gov" &&
                    UseFIPS === true &&
                    UseDualStack === true) {
                    return e(`https://streams-dynamodb-fips.${Region}.${_.getAttr(PartitionResult, "dualStackDnsSuffix")}`);
                }
                if (_.getAttr(PartitionResult, "name") === "aws-us-gov" &&
                    UseFIPS === true &&
                    UseDualStack === false) {
                    return e(`https://streams.dynamodb.${Region}.${_.getAttr(PartitionResult, "dnsSuffix")}`);
                }
                if (UseFIPS === true && UseDualStack === true) {
                    if (true === _.getAttr(PartitionResult, "supportsFIPS") &&
                        true === _.getAttr(PartitionResult, "supportsDualStack")) {
                        return e(`https://streams.dynamodb-fips.${Region}.${_.getAttr(PartitionResult, "dualStackDnsSuffix")}`);
                    }
                    return err("FIPS and DualStack are enabled, but this partition does not support one or both");
                }
                if (UseFIPS === true && UseDualStack === false) {
                    if (_.getAttr(PartitionResult, "supportsFIPS") === true) {
                        return e(`https://streams.dynamodb-fips.${Region}.${_.getAttr(PartitionResult, "dnsSuffix")}`);
                    }
                    return err("FIPS is enabled but this partition does not support FIPS");
                }
                if (UseFIPS === false && UseDualStack === true) {
                    if (true === _.getAttr(PartitionResult, "supportsDualStack")) {
                        return e(`https://streams.dynamodb.${Region}.${_.getAttr(PartitionResult, "dualStackDnsSuffix")}`);
                    }
                    return err("DualStack is enabled but this partition does not support DualStack");
                }
                return e(`https://streams.dynamodb.${Region}.${_.getAttr(PartitionResult, "dnsSuffix")}`);
            }
        }
    }
    return err("Invalid Configuration: Missing Region");
});
export class ExpiredIteratorException extends /*@__PURE__*/ /*@__PURE__*/ S.TaggedError()("ExpiredIteratorException", { message: S.optional(S.String).pipe(T.ErrorMessage()) }) {
}
export class InternalServerError extends /*@__PURE__*/ /*@__PURE__*/ S.TaggedError()("InternalServerError", { message: S.optional(S.String).pipe(T.ErrorMessage()) }) {
}
export class LimitExceededException extends /*@__PURE__*/ /*@__PURE__*/ S.TaggedError()("LimitExceededException", { message: S.optional(S.String).pipe(T.ErrorMessage()) }) {
}
export class ResourceNotFoundException extends /*@__PURE__*/ /*@__PURE__*/ S.TaggedError()("ResourceNotFoundException", { message: S.optional(S.String).pipe(T.ErrorMessage()) }) {
}
export class TrimmedDataAccessException extends /*@__PURE__*/ /*@__PURE__*/ S.TaggedError()("TrimmedDataAccessException", { message: S.optional(S.String).pipe(T.ErrorMessage()) }) {
}
export const ShardFilterType = /*@__PURE__*/ S.String;
export const ShardFilter = /*@__PURE__*/ S.suspend(() => S.Struct({
    Type: S.optional(ShardFilterType),
    ShardId: S.optional(S.String),
})).annotate({ identifier: "ShardFilter" });
export const DescribeStreamInput = /*@__PURE__*/ S.suspend(() => S.Struct({
    StreamArn: S.String,
    Limit: S.optional(S.Number),
    ExclusiveStartShardId: S.optional(S.String),
    ShardFilter: S.optional(ShardFilter),
}).pipe(T.all(ns, T.Http({ method: "POST", uri: "/" }), svc, auth, proto, ver, rules))).annotate({
    identifier: "DescribeStreamInput",
});
export const StreamStatus = /*@__PURE__*/ S.String;
export const StreamViewType = /*@__PURE__*/ S.String;
export const KeyType = /*@__PURE__*/ S.String;
export const KeySchemaElement = /*@__PURE__*/ S.suspend(() => S.Struct({ AttributeName: S.String, KeyType: KeyType })).annotate({
    identifier: "KeySchemaElement",
});
export const KeySchema = /*@__PURE__*/ S.Array(KeySchemaElement);
export const SequenceNumberRange = /*@__PURE__*/ S.suspend(() => S.Struct({
    StartingSequenceNumber: S.optional(S.String),
    EndingSequenceNumber: S.optional(S.String),
})).annotate({
    identifier: "SequenceNumberRange",
});
export const Shard = /*@__PURE__*/ S.suspend(() => S.Struct({
    ShardId: S.optional(S.String),
    SequenceNumberRange: S.optional(SequenceNumberRange),
    ParentShardId: S.optional(S.String),
})).annotate({ identifier: "Shard" });
export const ShardDescriptionList = /*@__PURE__*/ S.Array(Shard);
export const StreamDescription = /*@__PURE__*/ S.suspend(() => S.Struct({
    StreamArn: S.optional(S.String),
    StreamLabel: S.optional(S.String),
    StreamStatus: S.optional(StreamStatus),
    StreamViewType: S.optional(StreamViewType),
    CreationRequestDateTime: S.optional(S.Date.pipe(T.TimestampFormat("epoch-seconds"))),
    TableName: S.optional(S.String),
    KeySchema: S.optional(KeySchema),
    Shards: S.optional(ShardDescriptionList),
    LastEvaluatedShardId: S.optional(S.String),
})).annotate({
    identifier: "StreamDescription",
});
export const DescribeStreamOutput = /*@__PURE__*/ S.suspend(() => S.Struct({ StreamDescription: S.optional(StreamDescription) }).pipe(ns)).annotate({
    identifier: "DescribeStreamOutput",
});
export const GetRecordsInput = /*@__PURE__*/ S.suspend(() => S.Struct({ ShardIterator: S.String, Limit: S.optional(S.Number) }).pipe(T.all(ns, T.Http({ method: "POST", uri: "/" }), svc, auth, proto, ver, rules))).annotate({
    identifier: "GetRecordsInput",
});
export const OperationType = /*@__PURE__*/ S.String;
export const StringSetAttributeValue = /*@__PURE__*/ S.Array(S.String);
export const NumberSetAttributeValue = /*@__PURE__*/ S.Array(S.String);
export const BinarySetAttributeValue = /*@__PURE__*/ S.Array(T.Blob);
export const MapAttributeValue = /*@__PURE__*/ S.Record(S.String, S.suspend(() => AttributeValue)
    .annotate({ identifier: "AttributeValue" })
    .pipe(S.optional));
export const ListAttributeValue = /*@__PURE__*/ S.Array(S.suspend(() => AttributeValue).annotate({ identifier: "AttributeValue" }));
export const AttributeValue = /*@__PURE__*/ S.Union([
    S.Struct({ S: S.String }),
    S.Struct({ N: S.String }),
    S.Struct({ B: T.Blob }),
    S.Struct({ SS: StringSetAttributeValue }),
    S.Struct({ NS: NumberSetAttributeValue }),
    S.Struct({ BS: BinarySetAttributeValue }),
    S.Struct({
        M: S.suspend(() => MapAttributeValue).annotate({
            identifier: "MapAttributeValue",
        }),
    }),
    S.Struct({
        L: S.suspend(() => ListAttributeValue).annotate({
            identifier: "ListAttributeValue",
        }),
    }),
    S.Struct({ NULL: S.Boolean }),
    S.Struct({ BOOL: S.Boolean }),
]);
export const AttributeMap = /*@__PURE__*/ S.Record(S.String, S.suspend(() => AttributeValue)
    .annotate({ identifier: "AttributeValue" })
    .pipe(S.optional));
export const StreamRecord = /*@__PURE__*/ S.suspend(() => S.Struct({
    ApproximateCreationDateTime: S.optional(S.Date.pipe(T.TimestampFormat("epoch-seconds"))),
    Keys: S.optional(AttributeMap),
    NewImage: S.optional(AttributeMap),
    OldImage: S.optional(AttributeMap),
    SequenceNumber: S.optional(S.String),
    SizeBytes: S.optional(S.Number),
    StreamViewType: S.optional(StreamViewType),
})).annotate({ identifier: "StreamRecord" });
export const Identity = /*@__PURE__*/ S.suspend(() => S.Struct({ PrincipalId: S.optional(S.String), Type: S.optional(S.String) })).annotate({ identifier: "Identity" });
export const Record = /*@__PURE__*/ S.suspend(() => S.Struct({
    eventID: S.optional(S.String),
    eventName: S.optional(OperationType),
    eventVersion: S.optional(S.String),
    eventSource: S.optional(S.String),
    awsRegion: S.optional(S.String),
    dynamodb: S.optional(StreamRecord),
    userIdentity: S.optional(Identity),
})).annotate({ identifier: "Record" });
export const RecordList = /*@__PURE__*/ S.Array(Record);
export const GetRecordsOutput = /*@__PURE__*/ S.suspend(() => S.Struct({
    Records: S.optional(RecordList),
    NextShardIterator: S.optional(S.String),
}).pipe(ns)).annotate({
    identifier: "GetRecordsOutput",
});
export const ShardIteratorType = /*@__PURE__*/ S.String;
export const GetShardIteratorInput = /*@__PURE__*/ S.suspend(() => S.Struct({
    StreamArn: S.String,
    ShardId: S.String,
    ShardIteratorType: ShardIteratorType,
    SequenceNumber: S.optional(S.String),
}).pipe(T.all(ns, T.Http({ method: "POST", uri: "/" }), svc, auth, proto, ver, rules))).annotate({
    identifier: "GetShardIteratorInput",
});
export const GetShardIteratorOutput = /*@__PURE__*/ S.suspend(() => S.Struct({ ShardIterator: S.optional(S.String) }).pipe(ns)).annotate({
    identifier: "GetShardIteratorOutput",
});
export const ListStreamsInput = /*@__PURE__*/ S.suspend(() => S.Struct({
    TableName: S.optional(S.String),
    Limit: S.optional(S.Number),
    ExclusiveStartStreamArn: S.optional(S.String),
}).pipe(T.all(ns, T.Http({ method: "POST", uri: "/" }), svc, auth, proto, ver, rules))).annotate({
    identifier: "ListStreamsInput",
});
export const Stream = /*@__PURE__*/ S.suspend(() => S.Struct({
    StreamArn: S.optional(S.String),
    TableName: S.optional(S.String),
    StreamLabel: S.optional(S.String),
})).annotate({ identifier: "Stream" });
export const StreamList = /*@__PURE__*/ S.Array(Stream);
export const ListStreamsOutput = /*@__PURE__*/ S.suspend(() => S.Struct({
    Streams: S.optional(StreamList),
    LastEvaluatedStreamArn: S.optional(S.String),
}).pipe(ns)).annotate({
    identifier: "ListStreamsOutput",
});
/**
 * Returns information about a stream, including the current status of the stream, its Amazon Resource Name (ARN), the composition of its shards, and its corresponding DynamoDB table.
 *
 * You can call `DescribeStream` at a maximum rate of 10 times per second.
 *
 * Each shard in the stream has a `SequenceNumberRange` associated with it. If the
 * `SequenceNumberRange` has a `StartingSequenceNumber` but no
 * `EndingSequenceNumber`, then the shard is still open (able to receive more stream
 * records). If both `StartingSequenceNumber` and `EndingSequenceNumber`
 * are present, then that shard is closed and can no longer receive more data.
 */
export const describeStream = /*@__PURE__*/ API.make(() => ({
    input: DescribeStreamInput,
    output: DescribeStreamOutput,
    errors: [InternalServerError, ResourceNotFoundException],
    protocol: AwsProtocol,
    retry: Retry,
    operationName: "DescribeStream",
}));
/**
 * Retrieves the stream records from a given shard.
 *
 * Specify a shard iterator using the `ShardIterator` parameter. The shard iterator
 * specifies the position in the shard from which you want to start reading stream records
 * sequentially. If there are no stream records available in the portion of the shard that the
 * iterator points to, `GetRecords` returns an empty list. Note that it might take
 * multiple calls to get to a portion of the shard that contains stream records.
 *
 * `GetRecords` can retrieve a maximum of 1 MB of data or 1000 stream records,
 * whichever comes first.
 */
export const getRecords = /*@__PURE__*/ API.make(() => ({
    input: GetRecordsInput,
    output: GetRecordsOutput,
    errors: [
        ExpiredIteratorException,
        InternalServerError,
        LimitExceededException,
        ResourceNotFoundException,
        TrimmedDataAccessException,
    ],
    protocol: AwsProtocol,
    retry: Retry,
    operationName: "GetRecords",
}));
/**
 * Returns a shard iterator. A shard iterator provides information
 * about how to retrieve the stream records from within a shard. Use
 * the shard iterator in a subsequent
 * `GetRecords` request to read the stream records
 * from the shard.
 *
 * A shard iterator expires 15 minutes after it is returned to the requester.
 */
export const getShardIterator = /*@__PURE__*/ API.make(() => ({
    input: GetShardIteratorInput,
    output: GetShardIteratorOutput,
    errors: [
        InternalServerError,
        ResourceNotFoundException,
        TrimmedDataAccessException,
    ],
    protocol: AwsProtocol,
    retry: Retry,
    operationName: "GetShardIterator",
}));
/**
 * Returns an array of stream ARNs associated with the current account and endpoint. If the
 * `TableName` parameter is present, then `ListStreams` will return only the
 * streams ARNs for that table.
 *
 * You can call `ListStreams` at a maximum rate of 5 times per second.
 */
export const listStreams = /*@__PURE__*/ API.make(() => ({
    input: ListStreamsInput,
    output: ListStreamsOutput,
    errors: [InternalServerError, ResourceNotFoundException],
    protocol: AwsProtocol,
    retry: Retry,
    operationName: "ListStreams",
}));
//# sourceMappingURL=dynamodb-streams.js.map