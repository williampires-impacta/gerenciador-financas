import * as HttpClient from "effect/unstable/http/HttpClient";
import * as S from "@distilled.cloud/core/schema";
import * as API from "@distilled.cloud/core/api";
import { AwsProtocol } from "../protocol.js";
import { Retry } from "../retry.js";
import * as T from "../traits.js";
import * as C from "../category.js";
const svc = T.AwsApiService({
    sdkId: "SagemakerJobRuntime",
    serviceShapeName: "AgenticRFTRuntimeService",
});
const auth = T.AwsAuthSigv4({ name: "sagemaker" });
const ver = T.ServiceVersion("2026-02-01");
const proto = T.AwsProtocolsRestJson1();
const rules = T.EndpointResolver((p, _) => {
    const { UseFIPS = false, Endpoint, Region } = p;
    const e = (u, p = {}, h = {}) => ({
        type: "endpoint",
        endpoint: { url: u, properties: p, headers: h },
    });
    const err = (m) => ({
        type: "error",
        message: m,
    });
    if (Endpoint != null) {
        if (UseFIPS === true) {
            return err("Invalid Configuration: FIPS and custom endpoint are not supported");
        }
        return e(Endpoint);
    }
    if (Region != null) {
        {
            const PartitionResult = _.partition(Region);
            if (PartitionResult != null && PartitionResult !== false) {
                if (_.getAttr(PartitionResult, "name") === "aws" && UseFIPS === false) {
                    return e(`https://job-runtime.sagemaker.${Region}.${_.getAttr(PartitionResult, "dualStackDnsSuffix")}`);
                }
                if (_.getAttr(PartitionResult, "name") === "aws-cn" &&
                    UseFIPS === false) {
                    return e(`https://job-runtime.sagemaker.${Region}.${_.getAttr(PartitionResult, "dualStackDnsSuffix")}`);
                }
                if (_.getAttr(PartitionResult, "name") === "aws-us-gov" &&
                    UseFIPS === false) {
                    return e(`https://job-runtime.sagemaker.${Region}.${_.getAttr(PartitionResult, "dualStackDnsSuffix")}`);
                }
                if (UseFIPS === true) {
                    return e(`https://job-runtime.sagemaker-fips.${Region}.${_.getAttr(PartitionResult, "dualStackDnsSuffix")}`);
                }
                return e(`https://job-runtime.sagemaker.${Region}.${_.getAttr(PartitionResult, "dualStackDnsSuffix")}`);
            }
        }
    }
    return err("Invalid Configuration: Missing Region");
});
export class AccessDeniedException extends /*@__PURE__*/ /*@__PURE__*/ S.TaggedError()("AccessDeniedException", { message: S.String.pipe(T.ErrorMessage()) }, T.HttpError(403)).pipe(C.withAuthError) {
}
export class ConflictException extends /*@__PURE__*/ /*@__PURE__*/ S.TaggedError()("ConflictException", { message: S.String.pipe(T.ErrorMessage()) }, T.HttpError(409)).pipe(C.withConflictError) {
}
export class InternalServiceError extends /*@__PURE__*/ /*@__PURE__*/ S.TaggedError()("InternalServiceError", { message: S.String.pipe(T.ErrorMessage()) }, T.all(T.HttpError(500), T.Retryable())).pipe(C.withServerError, C.withRetryableError) {
}
export class ResourceNotFoundException extends /*@__PURE__*/ /*@__PURE__*/ S.TaggedError()("ResourceNotFoundException", { message: S.String.pipe(T.ErrorMessage()) }, T.HttpError(404)).pipe(C.withBadRequestError) {
}
export class ServiceQuotaExceededException extends /*@__PURE__*/ /*@__PURE__*/ S.TaggedError()("ServiceQuotaExceededException", { message: S.String.pipe(T.ErrorMessage()) }, T.HttpError(402)).pipe(C.withQuotaError) {
}
export class ThrottlingException extends /*@__PURE__*/ /*@__PURE__*/ S.TaggedError()("ThrottlingException", { message: S.String.pipe(T.ErrorMessage()) }, T.all(T.HttpError(429), T.Retryable())).pipe(C.withThrottlingError, C.withRetryableError) {
}
export class ValidationException extends /*@__PURE__*/ /*@__PURE__*/ S.TaggedError()("ValidationException", { message: S.String.pipe(T.ErrorMessage()) }, T.HttpError(400)).pipe(C.withBadRequestError) {
}
export const CompletionStatus = /*@__PURE__*/ S.String;
export const CompleteRolloutRequest = /*@__PURE__*/ S.suspend(() => S.Struct({
    JobArn: S.String.pipe(T.HttpHeader("X-Amzn-SageMaker-Job-Arn")),
    TrajectoryId: S.String,
    Status: S.optional(CompletionStatus),
    ClientToken: S.optional(S.String).pipe(T.IdempotencyToken()),
}).pipe(T.all(T.Http({ method: "POST", uri: "/complete-rollout" }), svc, auth, proto, ver, rules))).annotate({
    identifier: "CompleteRolloutRequest",
});
export const CompleteRolloutResponse = /*@__PURE__*/ S.suspend(() => S.Struct({})).annotate({
    identifier: "CompleteRolloutResponse",
});
export const SampleRequest = /*@__PURE__*/ S.suspend(() => S.Struct({
    JobArn: S.String.pipe(T.HttpHeader("X-Amzn-SageMaker-Job-Arn")),
    TrajectoryId: S.String.pipe(T.HttpHeader("X-Amzn-SageMaker-Trajectory-Id")),
    Body: T.StreamingInput.pipe(T.HttpPayload()),
}).pipe(T.all(T.Http({ method: "POST", uri: "/sample" }), svc, auth, proto, ver, rules))).annotate({ identifier: "SampleRequest" });
export const SampleResponse = /*@__PURE__*/ S.suspend(() => S.Struct({
    ContentType: S.optional(S.String).pipe(T.HttpHeader("Content-Type")),
    Body: T.StreamingOutput.pipe(T.HttpPayload()),
})).annotate({ identifier: "SampleResponse" });
export const SampleWithResponseStreamRequest = /*@__PURE__*/ S.suspend(() => S.Struct({
    JobArn: S.String.pipe(T.HttpHeader("X-Amzn-SageMaker-Job-Arn")),
    TrajectoryId: S.String.pipe(T.HttpHeader("X-Amzn-SageMaker-Trajectory-Id")),
    Body: T.StreamingInput.pipe(T.HttpPayload()),
}).pipe(T.all(T.Http({ method: "POST", uri: "/sample-with-response-stream" }), svc, auth, proto, ver, rules))).annotate({
    identifier: "SampleWithResponseStreamRequest",
});
export const SampleWithResponseStreamResponse = /*@__PURE__*/ S.suspend(() => S.Struct({
    ContentType: S.optional(S.String).pipe(T.HttpHeader("Content-Type")),
    Body: T.StreamingOutput.pipe(T.HttpPayload()),
})).annotate({
    identifier: "SampleWithResponseStreamResponse",
});
export const DoubleList = /*@__PURE__*/ S.Array(S.Number);
export const UpdateRewardRequest = /*@__PURE__*/ S.suspend(() => S.Struct({
    JobArn: S.String.pipe(T.HttpHeader("X-Amzn-SageMaker-Job-Arn")),
    TrajectoryId: S.String,
    Rewards: DoubleList,
    ClientToken: S.optional(S.String).pipe(T.IdempotencyToken()),
}).pipe(T.all(T.Http({ method: "POST", uri: "/update-reward" }), svc, auth, proto, ver, rules))).annotate({
    identifier: "UpdateRewardRequest",
});
export const UpdateRewardResponse = /*@__PURE__*/ S.suspend(() => S.Struct({})).annotate({
    identifier: "UpdateRewardResponse",
});
/**
 * Marks a rollout as complete, indicating that no further turns will be appended
 * to the trajectory. After calling this operation, the trajectory is sealed and
 * eligible for reward submission via the UpdateReward operation.
 */
export const completeRollout = /*@__PURE__*/ API.make(() => ({
    input: CompleteRolloutRequest,
    output: CompleteRolloutResponse,
    errors: [
        AccessDeniedException,
        ConflictException,
        InternalServiceError,
        ResourceNotFoundException,
        ServiceQuotaExceededException,
        ThrottlingException,
        ValidationException,
    ],
    protocol: AwsProtocol,
    retry: Retry,
    operationName: "CompleteRollout",
}));
/**
 * Sends an inference request to the model during a job execution. The request
 * and response bodies are forwarded to and from the model without modification.
 * Each turn (prompt and response) is captured for later use.
 */
export const sample = /*@__PURE__*/ API.make(() => ({
    input: SampleRequest,
    output: SampleResponse,
    errors: [
        AccessDeniedException,
        InternalServiceError,
        ResourceNotFoundException,
        ServiceQuotaExceededException,
        ThrottlingException,
        ValidationException,
    ],
    protocol: AwsProtocol,
    retry: Retry,
    operationName: "Sample",
}));
/**
 * Sends a streaming inference request to the model during a job execution.
 * Returns the response as a stream of payload chunks. Each turn is captured
 * for later use.
 */
export const sampleWithResponseStream = /*@__PURE__*/ API.make(() => ({
    input: SampleWithResponseStreamRequest,
    output: SampleWithResponseStreamResponse,
    errors: [
        AccessDeniedException,
        InternalServiceError,
        ResourceNotFoundException,
        ServiceQuotaExceededException,
        ThrottlingException,
        ValidationException,
    ],
    protocol: AwsProtocol,
    retry: Retry,
    operationName: "SampleWithResponseStream",
}));
/**
 * Updates the reward values for a trajectory and transitions it to
 * reward-received status, signaling that it is eligible for processing. Call this
 * operation after CompleteRollout to provide the computed reward scores.
 */
export const updateReward = /*@__PURE__*/ API.make(() => ({
    input: UpdateRewardRequest,
    output: UpdateRewardResponse,
    errors: [
        AccessDeniedException,
        ConflictException,
        InternalServiceError,
        ResourceNotFoundException,
        ServiceQuotaExceededException,
        ThrottlingException,
        ValidationException,
    ],
    protocol: AwsProtocol,
    retry: Retry,
    operationName: "UpdateReward",
}));
//# sourceMappingURL=sagemakerjobruntime.js.map