import * as HttpClient from "effect/unstable/http/HttpClient";
import * as S from "@distilled.cloud/core/schema";
import * as API from "@distilled.cloud/core/api";
import { AwsProtocol } from "../protocol.js";
import { Retry } from "../retry.js";
import * as T from "../traits.js";
const svc = T.AwsApiService({
    sdkId: "Marketplace Entitlement Service",
    serviceShapeName: "AWSMPEntitlementService",
});
const auth = T.AwsAuthSigv4({ name: "aws-marketplace" });
const ver = T.ServiceVersion("2017-01-11");
const proto = T.AwsProtocolsAwsJson1_1();
const rules = T.EndpointResolver((p, _) => {
    const { UseDualStack = false, UseFIPS = false, Endpoint, Region } = p;
    const e = (u, p = {}, h = {}) => ({
        type: "endpoint",
        endpoint: { url: u, properties: p, headers: h },
    });
    const err = (m) => ({
        type: "error",
        message: m,
    });
    if (Endpoint != null) {
        if (UseFIPS === true) {
            return err("Invalid Configuration: FIPS and custom endpoint are not supported");
        }
        if (UseDualStack === true) {
            return err("Invalid Configuration: Dualstack and custom endpoint are not supported");
        }
        return e(Endpoint);
    }
    if (Region != null) {
        {
            const PartitionResult = _.partition(Region);
            if (PartitionResult != null && PartitionResult !== false) {
                if (_.getAttr(PartitionResult, "name") === "aws" &&
                    UseFIPS === false &&
                    UseDualStack === true) {
                    return e(`https://entitlement-marketplace.${Region}.${_.getAttr(PartitionResult, "dualStackDnsSuffix")}`);
                }
                if (_.getAttr(PartitionResult, "name") === "aws-cn" &&
                    UseFIPS === false &&
                    UseDualStack === false) {
                    return e(`https://entitlement-marketplace.${Region}.amazonaws.com.cn`);
                }
                if (_.getAttr(PartitionResult, "name") === "aws-cn" &&
                    UseFIPS === false &&
                    UseDualStack === true) {
                    return e(`https://entitlement-marketplace.${Region}.${_.getAttr(PartitionResult, "dualStackDnsSuffix")}`);
                }
                if (_.getAttr(PartitionResult, "name") === "aws-eusc" &&
                    UseFIPS === false &&
                    UseDualStack === false) {
                    return e(`https://entitlement-marketplace.${Region}.amazonaws.eu`);
                }
                if (UseFIPS === true && UseDualStack === true) {
                    if (true === _.getAttr(PartitionResult, "supportsFIPS") &&
                        true === _.getAttr(PartitionResult, "supportsDualStack")) {
                        return e(`https://entitlement.marketplace-fips.${Region}.${_.getAttr(PartitionResult, "dualStackDnsSuffix")}`);
                    }
                    return err("FIPS and DualStack are enabled, but this partition does not support one or both");
                }
                if (UseFIPS === true && UseDualStack === false) {
                    if (_.getAttr(PartitionResult, "supportsFIPS") === true) {
                        return e(`https://entitlement.marketplace-fips.${Region}.${_.getAttr(PartitionResult, "dnsSuffix")}`);
                    }
                    return err("FIPS is enabled but this partition does not support FIPS");
                }
                if (UseFIPS === false && UseDualStack === true) {
                    if (true === _.getAttr(PartitionResult, "supportsDualStack")) {
                        return e(`https://entitlement.marketplace.${Region}.${_.getAttr(PartitionResult, "dualStackDnsSuffix")}`);
                    }
                    return err("DualStack is enabled but this partition does not support DualStack");
                }
                return e(`https://entitlement.marketplace.${Region}.${_.getAttr(PartitionResult, "dnsSuffix")}`);
            }
        }
    }
    return err("Invalid Configuration: Missing Region");
});
export class InternalServiceErrorException extends /*@__PURE__*/ /*@__PURE__*/ S.TaggedError()("InternalServiceErrorException", { message: S.optional(S.String).pipe(T.ErrorMessage()) }) {
}
export class InvalidParameterException extends /*@__PURE__*/ /*@__PURE__*/ S.TaggedError()("InvalidParameterException", { message: S.optional(S.String).pipe(T.ErrorMessage()) }) {
}
export class ThrottlingException extends /*@__PURE__*/ /*@__PURE__*/ S.TaggedError()("ThrottlingException", { message: S.optional(S.String).pipe(T.ErrorMessage()) }) {
}
export const GetEntitlementFilterName = /*@__PURE__*/ S.String;
export const FilterValueList = /*@__PURE__*/ S.Array(S.String);
export const GetEntitlementFilters = /*@__PURE__*/ S.Record(GetEntitlementFilterName, FilterValueList.pipe(S.optional));
export const GetEntitlementsRequest = /*@__PURE__*/ S.suspend(() => S.Struct({
    ProductCode: S.String,
    Filter: S.optional(GetEntitlementFilters),
    NextToken: S.optional(S.String),
    MaxResults: S.optional(S.Number),
}).pipe(T.all(T.Http({ method: "POST", uri: "/" }), svc, auth, proto, ver, rules))).annotate({
    identifier: "GetEntitlementsRequest",
});
export const EntitlementValue = /*@__PURE__*/ S.suspend(() => S.Struct({
    IntegerValue: S.optional(S.Number),
    DoubleValue: S.optional(S.Number),
    BooleanValue: S.optional(S.Boolean),
    StringValue: S.optional(S.String),
})).annotate({
    identifier: "EntitlementValue",
});
export const Entitlement = /*@__PURE__*/ S.suspend(() => S.Struct({
    ProductCode: S.optional(S.String),
    Dimension: S.optional(S.String),
    CustomerIdentifier: S.optional(S.String),
    CustomerAWSAccountId: S.optional(S.String),
    Value: S.optional(EntitlementValue),
    ExpirationDate: S.optional(S.Date.pipe(T.TimestampFormat("epoch-seconds"))),
    LicenseArn: S.optional(S.String),
})).annotate({ identifier: "Entitlement" });
export const EntitlementList = /*@__PURE__*/ S.Array(Entitlement);
export const GetEntitlementsResult = /*@__PURE__*/ S.suspend(() => S.Struct({
    Entitlements: S.optional(EntitlementList),
    NextToken: S.optional(S.String),
})).annotate({
    identifier: "GetEntitlementsResult",
});
/**
 * GetEntitlements retrieves entitlement values for a given product. The results can be
 * filtered based on customer identifier, AWS account ID, license ARN, or product dimensions.
 */
export const getEntitlements = /*@__PURE__*/ API.makePaginated(() => ({
    input: GetEntitlementsRequest,
    output: GetEntitlementsResult,
    errors: [
        InternalServiceErrorException,
        InvalidParameterException,
        ThrottlingException,
    ],
    protocol: AwsProtocol,
    retry: Retry,
    operationName: "GetEntitlements",
    pagination: {
        inputToken: "NextToken",
        outputToken: "NextToken",
        pageSize: "MaxResults",
    },
}));
//# sourceMappingURL=marketplace-entitlement-service.js.map