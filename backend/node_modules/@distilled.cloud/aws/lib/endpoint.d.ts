import * as Config from "effect/Config";
import * as Context from "effect/Context";
import * as Effect from "effect/Effect";
import * as Layer from "effect/Layer";
declare const Endpoint_base: Context.ServiceClass<Endpoint, "AWS::Endpoint", Effect.Effect<string | undefined, never, never>>;
export declare class Endpoint extends Endpoint_base {
}
/**
 * `AWS_ENDPOINT_URL` — the LocalStack-standard endpoint override that local
 * emulators (floci, LocalStack) inject into runtime environments (e.g.
 * Lambda containers). Resolves `undefined` when unset, so the SDK's
 * default endpoint resolver runs.
 */
export declare const fromEnvironment: Effect.Effect<string | undefined, Config.ConfigError, never>;
/** Override the endpoint with whatever the environment names, if anything. */
export declare const fromEnv: () => Layer.Layer<Endpoint, never, never>;
/** Override the endpoint for a scope, e.g. `Endpoint.of("http://localhost:4566")`. */
export declare const of: (endpoint: string) => Layer.Layer<Endpoint, never, never>;
export {};
//# sourceMappingURL=endpoint.d.ts.map