/**
 * @distilled.cloud/neon — Neon SDK for Effect.
 *
 * `./services` is generated by `scripts/generate.ts` from the Smithy model
 * in `.generated-specs` (written by `scripts/convert.ts` from the OpenAPI
 * spec). Everything else in this folder is hand-written.
 *
 * @example
 * ```ts
 * import * as Neon from "@distilled.cloud/neon";
 *
 * const projects = yield* Neon.Services.neon.listProjects({});
 * ```
 */
export * from "./credentials.js";
export * from "./errors.js";
export * as T from "./traits.js";
export { NeonProtocol, } from "./protocol.js";
export { paginateCursor } from "./pagination.js";
export * as Retry from "./retry.js";
export * as Services from "./services/index.js";
// v0-parity surface: operations (and their request/response types) are
// importable straight off the package root, as consumers already do.
// The service-local typed error classes shadow the same-named shared ones
// from ./errors.ts — the ops actually raise the service-local classes.
export * from "./services/neon.js";
export { BadRequest, Conflict, Forbidden, NotFound, UnprocessableEntity, } from "./services/neon.js";
//# sourceMappingURL=index.js.map