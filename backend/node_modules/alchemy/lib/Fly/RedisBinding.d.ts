import * as Effect from "effect/Effect";
import type { RuntimeContext } from "../RuntimeContext.ts";
import { RedisCommandError, RedisUrlMissing, type Redis } from "./Redis.ts";
export declare const makeRedisBinding: <Client>(options: {
    makeClient: (url: Effect.Effect<string, RedisUrlMissing, RuntimeContext>) => Client;
}) => Effect.Effect<(redis: Redis) => Effect.Effect<Client, never, never>, never, never>;
export declare const redisCommand: (url: Effect.Effect<string, RedisUrlMissing, RuntimeContext>, command: string, args?: readonly string[]) => Effect.Effect<unknown, RedisCommandError | RedisUrlMissing, RuntimeContext>;
//# sourceMappingURL=RedisBinding.d.ts.map