import * as p from "@clack/prompts";
import * as Effect from "effect/Effect";
declare const PromptCancelled_base: new <A extends Record<string, any> = {}>(args: import("effect/Types").VoidIfEmpty<{ readonly [P in keyof A as P extends "_tag" ? never : P]: A[P]; }>) => import("effect/Cause").YieldableError & {
    readonly _tag: "PromptCancelled";
} & Readonly<A>;
export declare class PromptCancelled extends PromptCancelled_base {
}
export declare const retryIfCancelled: <A, R>(self: Effect.Effect<A, unknown, R>) => Effect.Effect<A, unknown, R>;
/**
 * Wraps a clack prompt (which returns a `Promise<T | symbol>` where the
 * symbol indicates user cancellation) in an Effect.
 *
 * Returns `undefined` if the user cancels (Ctrl+C / Escape).
 *
 * Uses `Effect.callback` so fiber interruption propagates to the clack
 * prompt through its abort signal, releasing stdin and restoring the cursor.
 */
export declare const prompt: <T>(fn: (signal: AbortSignal) => Promise<T | symbol>) => Effect.Effect<T, PromptCancelled>;
export declare const success: (str: string) => Effect.Effect<void, never, never>;
export declare const warn: (str: string) => Effect.Effect<void, never, never>;
export declare const error: (str: string) => Effect.Effect<void, never, never>;
export declare const info: (str: string) => Effect.Effect<void, never, never>;
type TextOptions = Omit<p.TextOptions, "validate"> & {
    validate?: (value: string) => string | Error | undefined;
};
type PasswordOptions = Omit<p.PasswordOptions, "validate"> & {
    validate?: (value: string) => string | Error | undefined;
};
export declare const text: (opts: TextOptions) => Effect.Effect<string, PromptCancelled, never>;
export declare const password: (opts: PasswordOptions) => Effect.Effect<string, PromptCancelled, never>;
export declare const select: <Value>(opts: p.SelectOptions<Value>) => Effect.Effect<Value, PromptCancelled, never>;
export declare const confirm: (opts: p.ConfirmOptions) => Effect.Effect<boolean, PromptCancelled, never>;
export declare const multiselect: <Value>(opts: p.MultiSelectOptions<Value>) => Effect.Effect<Value[], PromptCancelled, never>;
/**
 * Open a URL in the user's default browser.
 *
 * On Windows, uses rundll32's FileProtocolHandler — a built-in shim that
 * opens URLs in the default browser. It accepts the URL as a direct
 * argument (no shell, no quoting of `&`). cmd.exe `start` would treat
 * `&` in OAuth URLs as a command separator, and `explorer.exe` treats
 * its arg as a path.
 */
export declare const openUrl: (url: string) => Effect.Effect<void, import("effect/PlatformError").PlatformError, import("effect/unstable/process/ChildProcessSpawner").ChildProcessSpawner>;
export {};
//# sourceMappingURL=Clank.d.ts.map