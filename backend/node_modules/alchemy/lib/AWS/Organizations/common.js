import * as organizations from "@distilled.cloud/aws/organizations";
import * as Effect from "effect/Effect";
import * as Schedule from "effect/Schedule";
import { createPhysicalName } from "../../PhysicalName.js";
import { createInternalTags, diffTags } from "../../Tags.js";
export const createName = (id, providedName, maxLength) => providedName
    ? Effect.succeed(providedName)
    : createPhysicalName({
        id,
        maxLength,
    });
export const toTagRecord = (tags) => Object.fromEntries((tags ?? [])
    .filter((tag) => typeof tag.Key === "string" && typeof tag.Value === "string")
    .map((tag) => [tag.Key, tag.Value]));
export const collectPages = (fetch, select) => Effect.gen(function* () {
    const items = [];
    let nextToken;
    do {
        const page = yield* fetch(nextToken);
        items.push(...(select(page) ?? []));
        nextToken = page.NextToken;
    } while (nextToken);
    return items;
});
export const retryOrganizations = (effect) => effect.pipe(Effect.retry({
    while: (error) => error?._tag === "ConcurrentModificationException" ||
        error?._tag === "TooManyRequestsException" ||
        error?._tag === "ServiceException" ||
        error?._tag === "FinalizingOrganizationException",
    schedule: Schedule.max([Schedule.exponential(200), Schedule.recurs(8)]),
}));
export const createManagedTags = Effect.fn(function* (id, tags) {
    return {
        ...(yield* createInternalTags(id)),
        ...tags,
    };
});
export const readResourceTags = (resourceId) => collectPages((NextToken) => organizations.listTagsForResource({ ResourceId: resourceId, NextToken }), (page) => page.Tags).pipe(Effect.map(toTagRecord));
export const updateResourceTags = Effect.fn(function* ({ id, resourceId, olds, news, }) {
    const oldTags = yield* createManagedTags(id, olds);
    const newTags = yield* createManagedTags(id, news);
    const { removed, upsert } = diffTags(oldTags, newTags);
    if (removed.length > 0) {
        yield* retryOrganizations(organizations.untagResource({
            ResourceId: resourceId,
            TagKeys: removed,
        }));
    }
    if (upsert.length > 0) {
        yield* retryOrganizations(organizations.tagResource({
            ResourceId: resourceId,
            Tags: upsert,
        }));
    }
    return newTags;
});
//# sourceMappingURL=common.js.map