/** @effect-diagnostics anyUnknownInErrorContext:off */
/**
 * INTERNAL — shared change-trigger and task-restart machinery for the floci
 * ECS dev providers ([FlociTaskProvider](./FlociTaskProvider.ts),
 * [FlociServiceProvider](./FlociServiceProvider.ts)). NOT exported from the
 * service `index.ts`.
 */
import * as ecs from "@distilled.cloud/aws/ecs";
import * as Effect from "effect/Effect";
import * as FileSystem from "effect/FileSystem";
import * as Path from "effect/Path";
import * as Stream from "effect/Stream";
import { type ImageSourceLike } from "../ECR/ImageSource.ts";
/**
 * A stream of "the image source changed" triggers for a props bag:
 *
 * - `main` — a `Bundle.watch` over the exact module graph the deploy
 *   bundles (rebuild successes are the trigger; rebuild errors are logged
 *   and swallowed);
 * - `context` — a debounced recursive `fs.watch` of the build context (and
 *   the Dockerfile's directory when it lives outside the context);
 * - `image` (registry ref) / inline-dockerfile-only — nothing local to
 *   watch: an empty stream.
 */
export declare const imageSourceTrigger: (options: {
    /** Logical id, for log prefixes. */
    id: string;
    source: ImageSourceLike;
    isExternal: boolean | undefined;
}) => Effect.Effect<Stream.Stream<void, never, never>, import("effect/PlatformError").PlatformError, import("../../AlchemyContext.ts").AlchemyContext | import("../../Docker/Docker.ts").Docker | FileSystem.FileSystem | Path.Path>;
/**
 * Restart every RUNNING task of `family` that is not yet on
 * `nextTaskDefinitionArn`:
 *
 * - **standalone** tasks (launched via `RunTask`, `startedBy` ≠ the service
 *   scheduler) are stopped and re-run on the new revision in the same
 *   cluster/launch type;
 * - **service-managed** tasks (`startedBy: "ecs-svc"` in floci) are only
 *   stopped — the service scheduler relaunches them on the service's
 *   current (freshly-updated) task definition.
 *
 * Bounded: one `listClusters` page, one `listTasks`/`describeTasks` round
 * per cluster.
 */
export declare const restartFamilyTasks: (options: {
    family: string;
    nextTaskDefinitionArn: string;
    /** Restart the service scheduler's tasks instead of standalone ones. */
    serviceManaged: boolean;
}) => Effect.Effect<number, import("@distilled.cloud/aws/Errors").AccessDeniedException | ecs.AccessDeniedException | ecs.BlockedException | ecs.ClientException | ecs.ClusterNotFoundException | ecs.ConflictException | import("@distilled.cloud/aws/Errors").EndpointError | import("@distilled.cloud/aws/Errors").ExpiredTokenException | import("effect/unstable/http/HttpClientError").HttpClientError | import("@distilled.cloud/aws/Errors").IncompleteSignature | import("@distilled.cloud/aws/Errors").InternalFailure | ecs.InvalidParameterException | import("@distilled.cloud/aws/Errors").MalformedHttpRequestException | import("@distilled.cloud/aws/Errors").NoMatchingRuleError | import("@distilled.cloud/aws/Errors").NotAuthorized | import("@distilled.cloud/aws/Errors").OperationAborted | import("@distilled.cloud/aws/Errors").OptInRequired | ecs.PlatformTaskDefinitionIncompatibilityException | ecs.PlatformUnknownException | import("@distilled.cloud/aws/Errors").RequestAbortedException | import("@distilled.cloud/aws/Errors").RequestEntityTooLargeException | import("@distilled.cloud/aws/Errors").RequestExpired | import("@distilled.cloud/aws/Errors").RequestTimeoutException | ecs.ServerException | ecs.ServiceNotFoundException | import("@distilled.cloud/aws/Errors").ServiceUnavailable | import("@distilled.cloud/aws/Errors").ThrottlingException | import("@distilled.cloud/aws/Errors").UnknownAwsError | import("@distilled.cloud/aws/Errors").UnknownOperationException | import("@distilled.cloud/aws/Errors").UnrecognizedClientException | ecs.UnsupportedFeatureException | import("@distilled.cloud/aws/Errors").ValidationError | import("@distilled.cloud/aws/Errors").ValidationException, import("@distilled.cloud/aws/Credentials").Credentials | import("effect/unstable/http/HttpClient").HttpClient>;
//# sourceMappingURL=EcsDevWatch.d.ts.map